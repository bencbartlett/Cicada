# Decisions ledger

Locked choices, with dates and rationale. Follow the wall-project
discipline: one row per decision, revise explicitly, never silently.

| Date | Decision | Rationale |
|---|---|---|
| 2026-08-11 | Name: **Cicada**; releases use plain semantic versioning (v0.1, v0.2, …) | Versions should communicate scope and compatibility, nothing else; no themed release names |
| 2026-08-11 | **Code is the source of truth; the graph is a generated view** | The wall project's .gh audit found production code drifted 74 lines behind disk and one module existing *only* inside the binary blob — the graph-as-truth failure mode in its purest form |
| 2026-08-11 | **Two-layer file format**: top layer = restricted dataflow dialect (one binding per node, order-independent), node bodies = arbitrary code the canvas never renders; layout metadata in a sidecar the differ ignores | The Enso/Luna lesson: bidirectional mapping over an arbitrary language collapses into "IDE with extra steps"; a restricted top layer keeps the graph view faithful by construction |
| 2026-08-11 | **Shape/rank-typed wires with named axes** (`Curve` vs `[Curve]` vs `parts: Curve`); broadcasting only via explicit combinators (`map`, strict `zip`, `cross`, `flatten`, `nest`, `transpose`, …; doc 09); element nulls are typed (`Optional`) and slot-preserving | Grasshopper's tree matching + Item/List access caused every worst bug in the wall project; typed shapes make them compile errors ("red wire before anything solves") |
| 2026-08-11 | **Engine in Rust** — data model, geometry, type checker, scheduler, and stdlib nodes are one Rust core; **Python stays available at the script boundary** (subprocess pool, typed marshalling) | A GH-style stdlib invites fine-grained graphs (hundreds of nodes × thousands of elements) where Python's per-element dispatch, boxing, and GIL tax the interactive loop by whole seconds; Rust delivers the stated priorities (speed, parallelism with real cancellation, ADTs, compile-time checking, wgpu GPU path). Revises the v1-runtime-is-Python row, which assumed coarse kernel-dominated stages |
| 2026-08-11 | **Scheduler contract**: content-hash caching per node, minimal recompute, parallel topological execution, disk-persistent memoization, cancellation everywhere, progress + ETA | "Feel-fast" is runtime architecture; GH re-solves everything downstream on a single-threaded UI thread with no cancel — the actually-antiquated part |
| 2026-08-11 | **Script node mechanism**: the function signature IS the ports; docstring first line = node title; re-parse on save; signature changes turn stale wires into type errors, never silent deletions (refined 2026-08-19 with the stage-6 port: in Python the RETURN annotation declares the output ports — `-> "T"` = one port `out`, a dict literal `-> {"a": "T", …}` = multi-output returning a dict with exactly those keys, `-> None` = an exporter with no outputs — and `@cicada.node(…, effectful=True)` marks exporters, which never auto-run; the dataclass form mentioned in the node-ABI row is superseded by this) | GH dropping wires on parameter edits is one of its worst behaviors; the return annotation keeps "signature = ports" literal for multi-output Python nodes |
| 2026-08-11 | **AI provenance + verification**: AI-written nodes store their generating prompt; nodes may carry property tests; the canvas renders red when a contract fails on current data | Turns "vibe-coded" into "vibe-coded but continuously verified"; keeps the human's mid-level understanding intact |
| 2026-08-11 | **Rent all geometry kernels** behind one typed seam: Manifold via `manifold-csg` (mesh booleans; `manifold3d` on crates.io is its facade — probe-verified 2026-08-18: f64-native `from_mesh_f64`/`to_mesh_f64`, typed NotManifold refusal, byte-deterministic across processes), spade/delaunator (Voronoi/Delaunay), OCCT via `opencascade-rs` (B-rep + STEP — **v0.1**, revised 2026-08-19 to agree with the analytic-first row: linked against a **prebuilt OCCT 7.8.x** through `DEP_OCCT_ROOT` (conda-forge's pinned build per platform — Windows, macOS, Linux; the source build is never the default path); the binding is a git dependency or an own fork carrying only reviewed, minimal patches — never blind merges of open upstream PRs; the week-1 probe decides the fork and the CI shape; decided 2026-08-20 by the probe (docs/probes/occt-2026-08.md) and WP-A: the binding is an **own fork, `bencbartlett/opencascade-rs` branch `cicada`**, pinned by rev, whose patch stack is read line by line and carries provenance — the MSVC handle aliases (the source part of upstream PR #230, which upstream closed unmerged), honest BREP writers (real BinTools, explicit `with_triangles`/`with_normals`, a PINNED format version, readers that refuse a null shape), in-memory BREP bytes, a total exception boundary (`catch (...)` in cxx's `trycatch` hook — an OCCT failure is an `Err`, never an abort; a self-test proves every clause), the exception-safe modeling glue the seam calls, the OCCT source submodule DROPPED (a default build fetches ~6 MB, not ~580 MB) and `LGPL-2.1-only` manifests; PR #216's Windows static libs were NOT needed on the prebuilt shared path and stay a recorded patch. Prebuilt OCCT = **conda-forge 7.8.1** per platform (win-64 / linux-64 / osx-64 / osx-arm64), fetched by `tools/fetch_occt.py` into the user cache dir as ONE conda prefix with the full runtime closure (every file sha256-pinned in `tools/fetch_occt_manifest.json`; the script, not the binding, asserts the exact version; the prefix is re-verified on the warm path); `cicada-geom`'s `occt` cargo feature is OFF by default and default builds never touch OCCT; CI = an `occt (ubuntu)` job per PR and the three-OS matrix nightly. All OCCT calls run under one process-wide kernel lock in WP-A (shared `TShape`s make two `Solid`s on two threads a data race — reproduced); WP-B decides the sharing model before the scheduler fans OCCT nodes out. The shipped binary uses an own CI-built OCCT with `USE_FREETYPE`/`USE_FREEIMAGE` OFF before the first external release — conda's STEP closure (47 DLLs / 56 MB / 16 licences) is for development and CI), fidget (implicits; supersedes libfive), ttf-parser/rustybuzz (text outlines — stage 6 note, 2026-08-19: ttf-parser was declared unmaintained (RUSTSEC-2026-0192); the spike keeps it over BUNDLED, trusted font bytes only, with the advisory ignored for that reason in deny.toml, and swaps to skrifa/fontations when fonts become user-supplied; fonts are bundled in the stdlib binary and named by the `font` port — system font lookup never, reproducibility), glam/curvo/lyon/cavalier_contours (vectors, NURBS, 2D paths/offsets). Optional rescue backend: Rhino.Compute | Kernel robustness is decades of accumulated case-handling (SSI, tolerant topology, fillets); Parasolid is $30–100k+/yr and company-only; OCCT is free with known ceilings; per-op routing keeps no single kernel's failure modes owning the pipeline |
| 2026-08-11 | **Web-first UI: browser app (customized React Flow canvas + three.js viewport) talking to the Rust engine server** (`cicada serve`, local or remote — Onshape-style; binary WebSocket streaming); instanced rendering; display is a first-class edge with visible cost; backward picking (click geometry → node + element index) is a launch requirement, not polish; the desktop app is a thin Tauri wrapper bundling a local engine (v0.2); native wgpu viewer stays the far escape hatch | The web canvas ecosystem is the only mature node-editor substrate; web-first serves the Onshape-class use case and makes the desktop app a packaging exercise, not a second codebase; one docked window beats the GH/Rhino split. Supersedes the Tauri-in-process framing, and polyscope before it |
| 2026-08-11 | **Blender is the photorealistic render backend**, bridged via USD (attributes carry ids/axes/material bins), driven headless via `pip install bpy` (Cycles) | Don't build rendering twice; Cicada owns the fast preview, Blender owns beauty shots |
| 2026-08-11 | **Test corpus = the wall-piece pipeline** (field solver, colorizer, labels, pins, carve, packer, exporters) (revised 2026-08-19: the **regression corpus = every example with committed golden outputs**, run nightly; the wall is the first and lives at `examples/wall/` — ONE copy, which is also the app playground; engine-wide tooling (measurement harness, normalizer) lives in `tools/`; the wall is one project among many to come and is never the measure of which nodes to build) | Real 1,500-part production code with known bodies buried; porting it is the empirical answer to every performance claim |
| 2026-08-11 | **Deferred, explicitly**: 2D constraint sketcher (rent planegcs/SolveSpace's solver when needed), GH plugin-ecosystem parity, Class-A NURBS surfacing, native wgpu viewer | Scope discipline: the unbounded tail is editor polish and kernel parity; neither blocks the thesis |
| 2026-08-11 | **Editable canvas ships in v1**: GH-style drag-and-drop placement, wiring, and parameter editing; every canvas edit materializes as a text edit to the dialect layer | Bidirectional editing is safe over a restricted one-binding-per-node dialect — the Enso trap requires an arbitrary language. GH migrants need direct manipulation on day one; revises the read-only-canvas deferral |
| 2026-08-11 | **Data model: sum types + capability traits + refinement wrappers** (`Closed<Curve>`, `Planar<Curve>`); the wire-compatibility lattice (Circle <: Curve <: Geometry) lives in the checker, not host-language inheritance; generics are kind-preserving (Move: T → T); the element variable `E` of list nodes binds the element kind INCLUDING optionality (a `[T?]` flows through slot-preserving list nodes as `[T?]`, `item` of an absent slot is an absent element — never green at check time, red at run time; `E` binds kinds, never list depth, so `item`/`length` over `[[X]]` keep offering the `each()` chip; stage 6, 2026-08-19) | Closedness/planarity are data-dependent properties, so they are checked refinements entered via explicit conversions (red with offending IDs on failure), not subclasses; exhaustive enum matching forces every stdlib node to handle or explicitly reject every kind |
| 2026-08-11 | **Stdlib = ~130 macro-registered typed node functions** cloning GH's most-used set (docs/08); pure, deterministic, explicit seeds; the registry serializes to a JSON catalog driving the palette, the checker, and the AI (revised 2026-08-19, decided with Ben: v0.1 builds the **docs/08 tier S + tier 1 list in full** — GH migrants expect the most-used set whether or not an example uses it; tier 2 and anything beyond docs/08 needs evidence; **one node per file** under `crates/cicada-stdlib/src/<category>/`, categories = the ribbon tabs; a **standardized self-documenting node format** enforced by a conformance test — title line, description, one doc paragraph per port (a wrapped field doc is one port doc: the macro joins the first paragraph; C1 review, 2026-08-20), `# Panics` = the red contract, `# Examples` = a runnable `.cic` snippet CI solves, and a `gh = "…"` attribute naming the Grasshopper component it replaces (or `gh = none`); the same data serves agents over MCP via `cicada mcp` in v0.1. Status 2026-08-20: after C1 every docs/08 §2–§4 S+1 row is shipped except `cross` (needs a second type variable) and `squeeze` (data-dependent depth) — both wait for checker work and are tracked in docs/08 §4 and docs/17, so "in full" is a standing obligation, not yet a fact) | Signature-is-the-ports extended to the stdlib at compile time; the catalog is the machine-readable surface the AI composes from; one file per node keeps parallel agents conflict-free and an edit's context to ~100–200 lines |
| 2026-08-11 | **User code in two tiers, ports auto-derived in both** (revises the three-tier row — the lowered-Python transform tier is dropped): (1) Expression nodes — language-neutral math syntax (`z = x^2 + y^2`), free variables become input ports, the assignment target names the output; (2) script nodes in sibling files — **Rust by default, including AI-generated nodes**, compiled to sandboxed WASM (wasmtime: epoch preemption = hard cancellation; a crash costs one node); a **Python 3 script node** (full CPython subprocess, numpy/scipy/rhino3dm) exists like Grasshopper's | One language for the core — commit to Rust; Python remains an option at the edges, never in the core representation |
| 2026-08-11 | **GH tree vocabulary retired**: lists are lists (`[T]`, `[[T]]`); graft/flatten/simplify/Path Mapper replaced by standard typed combinators (`map`, strict `zip`, `cross`, `flatten`, `nest`, `squeeze`, `transpose`, `chunk`, `group_by`, `compact`); zip length-mismatch is an error with opt-in policies (`pad_last`, `repeat`, `truncate` — revised 2026-08-20 when they shipped as nodes (C1): the cyclic policy is the node `repeat` (GH Repeat Data), named `cycle` until then; the §1 `cycle` time param owns that name, and a policy is a data transformation, so it is a node with a `count` — `pad_last(list, count)` / `repeat(pattern, count)` / `truncate(list, count)`) | The tree/graft/branch model is GH's most-documented pain point (doc 09); standard programming terms transfer, positional path voodoo doesn't |
| 2026-08-11 | **Wiring is type-gated in the UI**: incompatible connections cannot be completed (blocked at drag, with a reason); liftable mismatches connect only through a recorded adapter chip; lifted ports wear persistent iteration badges (`map ×depth`, `zip`) and wire hover shows the exact pairing | "Red wire before anything solves" moved earlier still: the wrong wire never exists, and iteration is always visible, never guessed |
| 2026-08-11 | **Pipeline file = `.cic`, Cicada's own minimal dialect** — bindings and expressions only; all multi-statement code lives in script files; version pragma `# cicada 1`; tree-sitter grammar + editor extension ship with v0.1; layout in `name.cic.layout.json`; cache and lock local-only; `.ci` rejected (collides with continuous-integration conventions) | Commit to one core language instead of aping Python (revises the `.ci.py` choice); the grammar is tiny enough that first-party tooling beats borrowed highlighting; a Rust-source file was rejected on semantics, not syntax — the dialect needs forward references, `^` as power, axis annotations, and domain-quality wire errors rather than rustc trait diagnostics; named arguments come from the struct-in/struct-out ABI (doc 10) |
| 2026-08-11 | **Dialect grammar rules**: single assignment (names are node identity); one node call per binding — nested calls are "name the intermediate" errors; **all arguments are kwargs**, each marked required or optional — positional args are parse errors; canvas port display order is a sidecar UI mapping, never code; `each(arg)` is the textual form of the lift chip (multiple `each` = strict zip); params are constructor bindings (`slider`/`toggle`/`choice`/bare literals); operator RHS = one Expression node (`^` = power); axis renames via binding annotation (`cells: parts = …`) | Every node nameable, inspectable, cacheable; lifts and pairings visible in text forever; every port is labeled in the UI, so positional args buy nothing (doc 10) |
| 2026-08-11 | **Round-trip writer discipline**: every canvas gesture maps to a specified minimal text edit (slider drag = one numeric token); the writer never reformats, reorders, or realigns existing lines; deleting a node produces red unknown-name errors downstream, never cascade deletion; rename updates references + sidecar atomically; caches key on content hashes so renames never recompute | Small reviewable diffs are the product; GH's silent wire-dropping inverted into loud, local errors (doc 10) |
| 2026-08-11 | **Git integration is first-class UI**: status strip with per-node change markers, node-level visual graph diff (a rendering of the text diff — it can never disagree with `git diff`), commit from the app, per-node history; plain git underneath, no custom store | One-binding-per-line + the minimal-edit writer make line diffs = node diffs; determinism makes artifact diffs meaningful; diffability is a product feature, not a byproduct (doc 10) |
| 2026-08-11 | **Auto-layout-first sidecar on a unit grid**: deterministic, snappy auto-layout; the sidecar stores manual overrides only (integer grid cells; one unit = the port-row pitch; node sizes are integer units; movement grid-snapped — no alignment tools needed); deleting an override snaps back to auto; port display order is a sidecar mapping | Untouched nodes carry zero layout state, so the sidecar stays near-empty and near-auto by construction; grid-snap revisable if it chafes (doc 10) |
| 2026-08-11 | **Node ABI is struct-in/struct-out**: every node function takes one input struct with named fields and returns an output struct with named fields (bare return allowed for single-output nodes; port named `out`); `#[derive(Ports)]` + `#[node]` reflect fields → ports; optional port = field with a default; the same field names appear in Rust call sites, the JSON catalog, the canvas labels, and dialect kwargs | Rust has no keyword arguments for functions — input structs are how it gets them; one nomenclature end to end kills a whole class of mapping bugs; Python script nodes mirror the ABI with dataclasses (docs/08) |
| 2026-08-11 | **AI edit loop**: agents edit the pipeline text directly; order-independence means no insertion constraints (append anywhere; `fmt` beautifies); the checker is the inner loop — milliseconds, no solve, JSON diagnostics with suggested fixes; read tools ground agents in live graph state (what-feeds, wire summaries, catalog search, profiler); mechanical bulk changes go through AST-safe refactor primitives (rename, insert_between, extract_script — the same ops the canvas uses); edits land as git diffs with guarded tiers (deletes, renames, rewires, param values); exports never run without explicit human action | The dialect's restrictions are exactly what make agent edits cheap and safe — local semantics, unambiguous references, machine-checkable in milliseconds (doc 11) |
| 2026-08-11 | **Scheduler internals**: values immutable, blake3-hashed at construction, interned (same hash → same Arc → instanced draws); cache key = node version + input hashes + pairing shape (stdlib nodes carry explicit semantic versions; scripts hash source; NaN refused, −0.0 canonicalized); two-level disk store (content-addressed value store + memo table with status and cost samples); early cutoff falls out of content addressing; solve generations with ~30 ms debounce and supersession; rayon wavefront execution with per-element fan-out (~10–50 ms chunks), preview-first then critical-path priorities, adaptive cache granularity; element failures red-with-IDs by default, `on_error=skip` → Optional explicitly; cost-weighted progress/ETA from persisted samples; lock file = last-solve state, warm reopen computes nothing. *Revised 2026-08-20 (item 3b review)*: the memo log is **append-only in format** — `LogRecord` variants are only ever added, every older log replays under a newer engine, and the store root carries a `format` marker (`LOG_FORMAT`, bumped in the same commit as any new variant) so an older engine REFUSES a newer store instead of reading its records as corruption and truncating; node-level memo entries record their computation's cost (the cost model stays complete after a warm reopen); a node that stops because its generation was cancelled says so (`NodeError::cancelled`) and lands `cancelled` only under a cancelled token — every other error stays red, Esc or not | Cancellation and slider-drag supersession are nearly free because completed work is always already cached; steal salsa's concepts, build custom — static explicit DAG, disk persistence, and element granularity live outside salsa's sweet spot (doc 12) |
| 2026-08-11 | **Cache sizing & location**: in-memory value LRU defaults to 25% of engine-host RAM; disk store defaults to 32 GB per project, LRU-evicted; the store lives in the user cache directory on the engine host, never inside the project folder by default (cloud-synced project dirs must not churn; `.cicada-cache/` is an opt-in override); the lock file stays kilobytes — output hashes of the current state only, a pointer into the store | Target hardware is mid-to-high-end desktops/laptops or a server, so default big; the cache holds alternate edit states for warm scrubbing/undo; correctness never depends on cache contents (doc 12) |
| 2026-08-11 | **Long-running-node machinery**: per-node cost models (regression on input size features, samples persisted) drive everything — predicted <~10 ms runs single-threaded, above enables internal parallelism, >~1 s routes to a cancellable kernel-worker subprocess and shows an inline progress bar (real progress via relaxed atomics on chunk/leaf counters polled ~10 Hz; model-estimated progress for opaque kernel calls; throttled host API for scripts); N-ary associative ops (`#[node(associative)]`) decompose into balanced parallel reduction trees — fixed shape, byte-identical results, every branch cacheable; user scripts are hard-cancellable by construction (WASM epochs / subprocess kill) | Esc must work even mid-giant-boolean, and the critical-path union should parallelize instead of serializing the solve; one cost model feeds chunking, ETA, threading, routing, and progress (doc 12) |
| 2026-08-11 | **Undo/redo model**: one linear op log over pipeline text + sidecar; human editing happens through the canvas (text stays the reviewable truth, not a concurrent editing surface); continuous gestures coalesce (slider drag = one op on release); agent graph edits apply as one atomic labeled diff at inference end, rebased onto current text (order-independence + single assignment make conflicts rare and detectable); external file changes (git checkout/pull) insert a reload barrier that clears the ephemeral stack; undo never recomputes — restored states are cache hits (revised 2026-08-19, decided with Ben: each op stores a **state snapshot** — pipeline text + sidecar — never a per-gesture inverse edit, so any change from any source is one op and `#off`, unit conversion and agent batches are undoable for free; sidecar-only ops (moves, preview toggles) are undo steps; effectful runs are non-undoable and say so; multi-node gestures and **external agent edits go through one atomic `batch` operation**: the whole new text (+ optional sidecar and script files), a label, the actor, and the **base** text hash the caller read — refused when the base is stale or the text does not parse (red wires are a valid state), otherwise applied under the session lock as one persist (temp + rename), one op, one delta — never a partial state on disk or in any client; the MCP/agent path MUST use this endpoint, a direct disk write is the external-change path (reload barrier, stack cleared); Backspace no longer deletes nodes — `Del` only, as in GH) (names pinned 2026-08-20 when it shipped: the whole-file agent operation is the `apply_text` intent / `POST /api/edit/apply_text`, and `batch` is the canvas's list of write gestures — same atomicity for both: one persist, one op, one delta; a write that leaves text and sidecar identical is not an op) | Constraining the editing surfaces makes undo an op log instead of a swamp; agent batches stay separable from interleaved human actions; git is the durable history, session undo is not |
| 2026-08-11 | **App protocol (doc 13)**: engine server owns all authoritative state; browser sends gesture-level intents, receives authoritative deltas (optimistic apply, server wins); slider drags stream ephemeral `param_preview` messages with one real op on release; JSON control plane + generation-tagged binary frames for geometry (hash-driven instancing, pick-ID tables); single-writer lease per pipeline with live read-only observers; snapshot resync; `cicada serve` binds localhost with a Jupyter-style URL token and embeds the SPA in the binary (revised 2026-08-19 with the stage-5 implementation: normals are derived on the GPU — flat shading — not transmitted; instancing is hash-driven with identity transforms in the spike; wire-drag compatibility is a server read intent evaluated by the checker; the SPA embeds behind the `embed` build feature — release + CI — with `--web-dir` and the Vite dev proxy as the explicit alternatives) | One hydration path, no merge machinery, stale frames unpaintable by construction; agents and humans enter the same op pipeline; remote deployment is the same binary behind a proxy |
| 2026-08-11 | **Interactive param behavior**: drags solve live with latest-wins supersession (no debounce on continuous streams), targeting 60 fps repaint on cheap cones; the cost model degrades expensive cones (≥ ~1 s) to compute-on-release automatically; sliders can opt into **scrub caching** — idle-priority, preemptible speculative solves warm the step-quantized range nearest-first within cache budget, shown as a video-style buffer bar on the slider (revised 2026-08-19, decided with Ben: **compute-on-release lands first** in v0.1; scrub caching lands after the time transport and is offered only for sliders whose step-quantized range has a **bounded position count** — 0…1 by 0.1 qualifies, by 0.01 does not; the threshold is set when the item starts — per-slider toggleable and off by default; revised 2026-08-20, the web-half review: the pointer released on the committed value is an intent, `end_drag`, and the server announces the end of every announced compute-on-release drag with `drag_ended` — after the delta/error/snapshot when there is one — so observers and the non-dragging twin widget take the badge down too; a pause longer than the drag gap is NOT announced, a pause being no release — docs/13 §Slider drags) | Smooth parameter tweaking is the point of a parametric tool; content addressing makes warming trivially incremental (skip-if-stored) and needs zero invalidation bookkeeping (docs 12, 13) |
| 2026-08-11 | **Time is a transport-driven param, never ambient**: `Cycle` (0→1 looping, adjustable period, frame-quantized — one pass warms the cache, then playback is pure cache reads) and `Clock` (unbounded, deterministic per value, uncached) are first-class stdlib params with a server-authoritative global transport (play/pause/speed/reset as intents); GH-style Timer stays absent (revised 2026-08-21, v0.1 item 4 as shipped: the intents are `transport_play` / `transport_pause` / `transport_seek {frame}` / `transport_speed {factor}` (≤ 64×) / `transport_reset`, **writer-only** — playback is shared session state every client sees and the lease is the one arbiter of shared state; observers follow and take the lease to drive; Esc and the last client leaving pause it; the playhead is injected at LOWERING into the `transport_driven` ports and never written to the file, so headless runs evaluate the text's own values — frame 0, t 0) | Kinetic/animated models are a first-class use case (doc 01 §6); the graph stays pure — the player feeds values, so determinism, caching, and observers' synchronized playback all survive animation (docs 08, 12, 13) |
| 2026-08-11 | **Workspace & toolchain (doc 14)**: nine-crate cargo workspace with dependency direction as law (core ← geom/lang/stdlib/sched/script ← server ← cli; parser+checker share `cicada-lang`; stdlib never depends on sched); Rust stable, edition 2024; lints = clippy all + pedantic (curated allows), `-D warnings`, unwrap/expect denied in library code, overflow-checks on in all profiles; thiserror in libraries, anyhow only in cli; postcard for cache/WASM buffers, MessagePack for the Python boundary, JSON for control plane; meshes are SoA flat f64 Arc buffers hashed at construction; curves stay analytic — tessellation is a derived cached op; no dtype-generic geometry (f64-only values; f32 lives in the display path and node-internal compute); corpus assets live in-repo | Crate boundaries double as agent work boundaries; heavy deps quarantined, churn isolated, core tiny; fail loudly and immediately; one serialization story per boundary |
| 2026-08-11 | **Analytic + B-rep first; meshing is post-processing** (revises the mesh-native-v0.1 framing): `Solid` is B-rep-backed (OCCT) from v0.1 — primitives, extrude/loft/revolve/sweep, booleans, STEP in/out; `Watertight<Mesh>` (Manifold-checked) is the mesh-tier solid for mesh-destined work; `Tessellate` is the explicit bridge; the wall corpus and spike stay on the mesh tier (revised 2026-08-19, decided with Ben: **B-rep is the default working mode** — the bare names `box`/`sphere`/`cylinder`/`cone`/`extrude`/`loft`/`revolve`/`sweep`/… output `Solid`; the shipped mesh-backed implementations keep the mesh tier under `mesh_*` names — `mesh_box`, `mesh_sphere`, `mesh_extrude`, `mesh_loft` (docs/08 §8 gains the rows); the rename lands in the SAME commit as the OCCT-backed nodes so no version lacks `box`; display tessellates Solids through a hash-keyed cache; the wall example stays on the mesh tier and its carve numbers are part of that commit's DoD. Supersedes the spike's 'mesh-backed under v0.1 names' shim. 2026-08-20, probe Q2 + WP-A: a `Solid` hashes its **canonical BinTools bytes** — written with `with_triangles = false`, `with_normals = false`, a PINNED `BinTools_FormatVersion` (never `_CURRENT`), the per-`TShape` Free/Modified/Checked history flags normalized, a single-solid `Compound` result unwrapped and a multi-solid one refused loudly — blake3 at construction like every other value; no geometric-summary hash. Display tessellation never feeds back into the bytes (the default-path overloads would embed it). Golden hashes of OCCT-backed values are committed **per platform** and collapse to one file only when the dedicated CI job shows win-64 / linux-64 / osx-arm64 agree for the transcendental-free corpus; a disagreement becomes a documented per-OS golden, never a looser comparison) | The GH workflow this models is analytic/B-rep until the end, then tessellate once; wall lesson 6 still holds — mesh-destined work uses Manifold booleans; B-rep booleans carry OCCT's known ceilings + the Rhino.Compute rescue hatch; byte identity of the canonical bytes held across processes and across two independently compiled OCCT 7.8.1 builds on one machine (box / extrude / boolean / loft; the round trip is a fixed point; the header carries no run-dependent bytes) — but not yet across libm/FMA differences between OSes, hence per-OS goldens until the CI matrix proves them universal |
| 2026-08-11 | **Units are changeable, two ways**: relabel (1 mm → 1 in, numbers untouched) or convert (1 mm → 0.03937 in, numbers rescaled); convert rewrites literals feeding length-dimensioned ports (`#[port(dimension = length)]`) including slider ranges, as one undoable op; unconvertible cases (expression free variables) are flagged for review, never silently scaled | Unitless numbers can't be converted blindly — dimension tags on ports make conversion exact where it's safe and loud where it isn't (doc 14) |
| 2026-08-11 | **Documentation pipeline**: doc comments are the single source; `docs/generated/CATALOG.md` (one line per registered node, committed, CI-checked fresh) is what agents read instead of grepping crates; `cicada docs` generates the human reference from the same comments, with runnable examples CI executes; `cicada catalog` merges project script nodes (revised 2026-08-18: script merging happens at SOLVE time today — `cicada run` discovers `scripts/*.py` into the live catalog; the project-aware `catalog` subcommand output lands with the server's project context, stage 5+; revised 2026-08-19: `# Examples` is a REQUIRED node-doc section — runnable, CI-solved — and `cicada mcp` serves catalog search, node docs and the checker to agents in v0.1 from the same data as `/api/catalog`) | Agents burn context grepping — a few KB of catalog replaces thousands of lines of source; generated docs can't drift from code; examples that stop solving fail the build (doc 14) |
| 2026-08-11 | **Spike scope locked (doc 15)**: six stages — scaffold-with-standards, values+registry, dialect+checker-lite, scheduler-lite, mesh-tier geometry + ~30 S-nodes + Python script host, server+canvas+viewport, then the wall slice measured; explicitly out: OCCT, WASM host, undo, git panel, scrub caching, time transport, AI layer; spike Extrude/primitives are mesh-backed under v0.1 names; kill/pivot criteria written down | The gate stays empirical: five criteria with a written measurement protocol; every subsystem ships its thinnest honest slice, nothing mocked that the criteria depend on |
| 2026-08-11 | **UI contracts locked at structure level (doc 16)**: single docked window — GH-style category ribbon on top (tabs = the docs/08 categories, catalog-driven, drag/click to place), canvas/viewport resizable split with presets, inspector, read-only synced text panel in v1; Rhino-style viewport navigation default (Blender preset + remapping); four-tier zoom LOD ending in port data previews (GH's Sunglasses plugin, native — cached wire summaries + content-addressed lazy thumbnails); double-click search-to-place (plain substring v1, fuzzy later) and drag-wire-to-empty-canvas type-filtered search; every stdlib node carries an AI-pipeline-generated icon (category-hued, CI-checked) with the function name in small text over the node — spike uses fallback glyphs; wire display setting: spline (GH-traditional, default) or 45°-increment PCB-trace routing snapped to the grid; one status vocabulary = the scheduler states; no save button — ops persist immediately, Ctrl+S opens the commit dialog; dark-first theming with semantic-only color; per-user settings never touch project semantics | Consolidates the UI decisions scattered across docs 02/04/09/10/12/13 so stage-5 agents build one coherent app; pixel-level aesthetics deliberately unspecified — they iterate in code behind these contracts, keeping v1 out of the editor-polish swamp |
| 2026-08-11 | **Node disable is native**: the canvas toggle prefixes the statement with `#off ` — a comment to generic tools, but Cicada parses the binding, ghosts the node with wiring intact, and skips it in solves; downstream reds with the precise reason ("fed by disabled node"), never unknown-name; re-enabling is usually a pure cache hit | Honest text form for GH's most-used workflow toggle; red-not-ghost downstream is acceptable precisely because content addressing makes the round trip free (docs 10, 16) |
| 2026-08-11 | **Groups are first-class but purely visual** (sidecar state): create/ungroup, title + color, collapse as pure rendering (members persist; outside wires draw to the tile), batch move/select/disable. **No group-compilation**: fusing mapped chains (the CUDA-graphs instinct) is a scheduler optimization to take automatically on profiling evidence, orthogonal to grouping; `extract_script` (doc 11) is the sanctioned path when a group should become semantics | Per-node overhead is already nanoseconds, so group-compilation buys little automatic fusion can't; tying performance to a differ-ignored file would break the sidecar contract ("deleting it loses only aesthetics") (docs 10, 12, 16) |
| 2026-08-11 | **Tolerance & units**: document unit defaults to mm; one `ProjectConfig` carries `tol` (1e-6 units) and `tol_angle` (1e-9 rad), configurable per project; its **tolerance hash** (tol + tol_angle, unit excluded — a relabel must not dirty tolerance caches) is **hashed into the NodeKey of every node that declares `uses_tolerance`**; the sanctioned comparison API is the only float-comparison path in geometry code; raw float `==` is lint-banned in geometry code — exact `==` is sanctioned in hash/determinism tests and in stdlib tests whose node contract is exact IEEE arithmetic (revised 2026-08-12: exact-IEEE test carve-out + unit excluded from the tolerance hash) | Tolerance is explicit state, never ambient — the no-ambient-state rule survives CAD; changing tolerance correctly invalidates exactly the caches it affects (doc 14) |
| 2026-08-11 | **License: all rights reserved while private**; the real license is chosen at first public release, when product shape and any commercial intent are clear | Deferring costs nothing while the repo is private; switching becomes costlier as code volume grows, so the decision point is the go-public moment, not later |
| 2026-08-11 | **Dev standards for a human + agent fleet (doc 14)**: per-layer test standards (every stdlib node: table cases + property test + golden-hash determinism test; dialect: byte-identical round-trip fixtures per gesture; scheduler: virtual-time fake nodes; e2e: Playwright smoke + nightly wall corpus with hash comparison); CI gates fmt/clippy/tests/cross-platform checks per PR, corpus + benchmarks nightly; root CLAUDE.md/AGENTS.md + skills (verify-change, add-stdlib-node, dialect-change, protocol-change, perf-check); the verification loop is agent-operated — headless `cicada run` first, Playwright + debug endpoints for UI, evidence attached; bug fixes land with their regression test in the same commit | Agents retain no context between conversations, so the repo carries the operating manual; the human reviews evidence and is never the feedback loop |
| 2026-08-11 | **Numeric precision policy**: f64 is canonical for all geometry semantics on CPU (curves, tolerances, booleans, exports); f32 is derived for display buffers, with origin-rebasing for large coordinates. **GPU acceleration is not a v1 feature**: CPU paths are the baseline everywhere; wgpu compute is a per-op optimization taken later where profiling justifies it (no dependence on GPU f64 — consumer cards throttle it 1/32–1/64 and WebGPU lacks it). Kernels stay rented via FFI — transpiling C++ kernels to Rust is rejected (an unmaintainable fork that forfeits upstream fixes); Cicada owns the value-type layouts with zero-copy seams | CAD exactness lives CPU-side in f64, and most CAD isn't GPU-accelerated anyway — defer the complexity until it pays; verify Manifold's precision model at the seam during the spike |
| 2026-08-19 | **v0.1 order, decided with Ben**: (1) undo/redo with the atomic `batch` path, riders `#off` and Backspace-no-longer-deletes; (2) git panel slice 1 — status strip, per-node change markers, commit and revert-to-HEAD over the git binary via HTTP routes (graph-diff overlay and per-node history follow once the markers have weeks of use); (3) OCCT-backed Solid as the main geometry track from week 3, after a week-1 probe (hours, capped at one day: prebuilt-OCCT build/link on Windows + macOS + Linux, byte determinism of serialized B-reps across processes and OSes, op timings at 1/100/1,000 parts, license + deny rows, CI shape), with the scheduler foundations (per-solve cancel handle, `volatile` flag, idle-class hypothetical solve) + compute-on-release running in parallel in the other crates; (4) time transport (Cycle thin slice with an orbit example; Clock via `volatile`); (5) scrub caching; (6) WASM script host last — it has no consumer until the v0.2 AI layer. The catalog runs in parallel from day 1: the one-node-per-file restructure first, then the docs/08 S+1 list in tranches, plus `cicada mcp`. `docs/17-v01-plan.md` is the work order; work packages and sessions are sized so that no single context must hold an item, and every package ends in a commit | The spike's measurements argue it: cheap cones are already sub-ms and undo never recomputes, so undo and git are pure text/protocol work with daily usage pressure; B-rep as the default mode makes OCCT a firm v0.1 item whose long lead is bought by the early probe and a parallel track; transport, scrub caching and WASM have no consumer in the repo yet |
| 2026-08-20 | **OCCT licensing.** OCCT 7.8.1 is `LGPL-2.1-only` **with the Open CASCADE Exception 1.0**: object code that incorporates material from OCCT header files (inline functions, templates, "CDL generic classes") may be distributed "under terms of your choice, provided that you give prominent notice in supporting documentation" that it uses OCCT. The exception covers exactly what our cxx glue and the `Handle<>`/`gp_*` inlines put into *our* object code; it does **not** relax LGPL §6 for the OCCT libraries themselves. *While private*: nothing is conveyed, nothing is triggered; the only obligation is hygiene (keep `LICENSE_LGPL_21.txt` + `OCCT_LGPL_EXCEPTION.txt` in the repo's third-party notices, record the exact build we link). *At go-public, shipping binaries*: **dynamic linking** (the conda `TK*.dll` / `libTK*.so` / `libTK*.dylib` beside the executable) is LGPL §6(b)'s "suitable shared library mechanism" — obligations are the notices, the prominent OCCT acknowledgement, and making the *corresponding source of the exact OCCT build* available (for conda-forge that is upstream `V7_8_1` + the feedstock's 16 patches at feedstock commit `4237a64ca19105111b57b8d18535e9de717d8870`, both public — pin that commit next to the package sha256s). **Static linking** (an own static prebuilt, or `builtin`) falls under §6(a): we would have to provide Cicada's own object files (or another relinkable form) so a user can relink against a modified OCCT — acceptable only if Cicada itself is open source at that point, and the reason static is not the v0.1 default. *The Rust binding is LGPL too*: `opencascade`, `opencascade-sys` (and the stray `kicad-parser`) are `LGPL-2.1` crates, and Rust links them **statically** — so the §6(a) analysis applies to the glue regardless of how OCCT itself is linked. If Cicada goes public as a proprietary binary, the seam must be Cicada-owned code (the surface Item 3 needs is ~25 functions: primitives, prism/loft/revolve/sweep, the three booleans, `BRepMesh_IncrementalMesh` + triangulation read-out, `BinTools` with explicit flags, STEP read/write) — the OCCT exception then covers our object code, and only the OCCT libraries stay LGPL. Decide the dependency shape (fork vs. own seam) with this in view; it does not block v0.1. *If the shipped binary carries conda-forge's STEP closure* (§Q1): the 21 extra DLLs bring 16 more notices — FreeType (`FTL` — choose it over the GPL-2.0 alternative; requires the FreeType credit), FreeImage (choose the **FreeImage Public License**, not the GPL alternatives — FIPL permits proprietary use with the notice), LibRaw (`LGPL-2.1-only`, dynamic → §6(b), same as OCCT), and permissive notices for imath / openexr / libwebp / zstd (BSD-3), openjpeg (BSD-2), libjpeg-turbo (IJG + BSD-3 + Zlib), libpng (zlib-acknowledgement), libtiff (HPND), libdeflate / lcms2 (MIT), lerc (Apache-2.0), liblzma (0BSD), zlib (Zlib). None blocks, all must be in the third-party notices; an own-built OCCT with `USE_FREETYPE`/`USE_FREEIMAGE` OFF removes all 16 (§Recommendation). (Verbatim from the probe memo docs/probes/occt-2026-08.md §4a, as §4d asked; it qualifies, not replaces, the 2026-08-11 row **License: all rights reserved while private** — the go-public license choice is still made there; this row is what that choice must accommodate. WP-A 2026-08-20: the fork is `bencbartlett/opencascade-rs@960a8bc`, Cicada depends on `opencascade-sys` only (the `LGPL-2.1-only` glue crate — the fork's manifests carry the current SPDX id for what upstream's deprecated bare `LGPL-2.1` meant — statically linked), `deny.toml` scopes the `LGPL-2.1-only` exception to that crate and the git source to the fork, and `tools/fetch_occt.py` extracts every package's license texts into `<prefix>/.cicada-occt-meta/<package>/info/licenses/` so the third-party notices can be assembled from the exact files shipped; the closure as pinned today is 29 / 49 / 28 / 28 packages for win-64 / linux-64 / osx-64 / osx-arm64 — more than the memo's 16 because the resolver also takes conda's compiler runtimes, X11 and fontconfig rather than relying on the host's — with every package's license id in `tools/fetch_occt_manifest.json`.) | LGPL-2.1 + the Open CASCADE exception; dynamic linking keeps OCCT under §6(b); the statically linked LGPL glue is the real exposure at go-public; conda's STEP closure adds 16 notices |
