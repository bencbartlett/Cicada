# OCCT probe — 2026-08 (docs/17 §Item P)

Written incrementally during the probe; every section carries the time it
was written. The verdict block is updated after every finding. A RED
verdict is a valid outcome.

Probe started 2026-08-19 23:44 PDT on Ben's Windows 11 machine
(rustc 1.97.1 MSVC, MSVC toolset 14.44.35207, Python 3.10.6, cmake from
VS Build Tools). A first session died after downloading and extracting
the package; this session resumed 2026-08-20 00:20 PDT from that
coherent partial state. Worktree `wt/occt-probe`, private
`CARGO_TARGET_DIR` `%LOCALAPPDATA%\cargo-target-wt\occt-probe`.
Probe project: `tools/probes/occt-2026-08/` (standalone cargo project,
its own `[workspace]` table). Download cache:
`%LOCALAPPDATA%\cicada-occt\`.

**Scope (a sanctioned narrowing, stated up front).** docs/17 §Item P
asks for win-64 first, then Linux and macOS through CI, and for the
determinism check to be "repeated across OSes in CI". This probe ran on
**win-64 only**: no Linux/macOS build, load-path or cross-OS hash
result exists here. The package brief scoped the probe to Ben's Windows
machine; the CI job that closes the gap is Item 3 WP-A's first task
(§4c), and every verdict cell below says "same machine" where that is
all it can say. The "Done when" of docs/17 is therefore met for win-64
and *drafted* (not measured) for the other two platforms.

**Revised 2026-08-20 03:10–04:00 after the adversarial review.** The
review found the §Q1 STEP DLL-closure chain wrong (hand-walked; it said
`TKCAF → TKV3d`, the real edge is `TKDESTEP → TKXCAF → TKV3d →
TKService → freetype/FreeImage`) and a handful of smaller defects. This
revision replaces the hand-walk with a tool
(`tools/probes/occt-2026-08/dll_closure.py`, pure-Python PE import
walker), fetches the runtime packages the STEP path actually needs and
measures the load set end to end (a second probe binary,
`occt-probe-step`), adds the loud format check to `dump`, verifies the
exception-at-the-cxx-boundary risk with a `throw` subcommand, surveys
the alternative bindings, and drafts the ledger rows docs/17 asks for
(§4d). Every changed number says so in place; the §Log entries keep
their original times.

## Verdict

| # | Question | Verdict | One line |
|---|---|---|---|
| 1 | Prebuilt build/link on Windows (MSVC) | **GREEN** (with one patch) — **AMBER for shipping STEP through conda's build** | conda-forge occt 7.8.1 + `opencascade-sys --no-default-features` compiles, links and RUNS with MSVC after the source-only part of upstream PR #230 (pure rename, 25 files, +131/−58). Runtime, measured with `dll_closure.py` and clean-PATH launches: a modeling-only binary needs **15 OCCT DLLs (30.0 MB)** beside the exe or on PATH; a **STEP-capable** binary needs **26 OCCT DLLs (44.8 MB) + 21 font/image-codec DLLs (11.1 MB) from 16 more conda packages = 47 DLLs, 56.1 MB**, because conda builds OCCT with `USE_FREETYPE`/`USE_FREEIMAGE` ON and `TKDESTEP → TKXCAF → TKV3d → TKService → freetype.dll + FreeImage.dll`. Without them a STEP-linking exe dies at load (`0xC0000135`); with them it runs (verified both ways) |
| 2 | Determinism of serialized B-reps across processes | **GREEN** (same machine) | box / extrude / difference / loft: BinTools V4 bytes, BRepTools text bytes, and tessellation buffers (positions+indices AND normals) are sha256-identical across two processes; re-read→re-write is byte-stable; no timestamps in either header; the same bytes again from a second, independently compiled OCCT 7.8.1 (static source build, MSVC 14.44, vs conda's MSVC 19.29 shared build — a different binary, heap layout and compiler, which is also the guard against a same-process-layout/ASLR false pass). Two binding defects found on the way (BinTools silently aliased to the text writer; `theWithTriangles` defaults to TRUE) — both must be fixed in the fork before `Solid` hashes canonical bytes. Cross-OS identity is NOT measured here (CI job) |
| 3 | Timings at 1 / 100 / 1,000 parts | **GREEN** | release, one thread, linear in parts: box 0.024 ms, extrude 0.096 ms, boolean difference 2.85 ms, tessellate 0.86 ms (deflection 0.01) per part → 1,000 parts ≈ 3.8 s total; exe 326 KB; relink 0.7 s; cold `cargo build --release` 28.5 s (23 s of it the glue) |
| 4 | Policy drafts (license, deny.toml, CI shape) | **GREEN** (drafted, verified where checkable) | LGPL-2.1 + OCCT exception row drafted; the exact `deny.toml` rows were run through cargo-deny 0.20.2 against the probe (`licenses ok, sources ok`); the CI shape is an `occt` feature in `cicada-geom` + one fetch-and-test job per OS using the four conda-forge 7.8.1 `_103` files named below — plus, as soon as a test binary links STEP, the 16 runtime packages of §4c extracted into the same prefix (otherwise the job dies at load, `0xC0000135`); cold cost ≈ +3 min per OS (estimate from a 24-thread desktop, not a runner measurement). Two policy risks surfaced: the Rust binding itself is LGPL and links statically; conda's STEP-capable closure carries 16 third-party licences incl. FreeImage (GPL/FIPL dual) and LibRaw (LGPL) |

Recommended path forward: **prebuilt conda-forge shared libs behind an `occt` feature for development and CI now, the binding in an own fork carrying the recorded patches; for the SHIPPED binary, an own CI-built OCCT artifact with `USE_FREETYPE`/`USE_FREEIMAGE` OFF (same `DEP_OCCT_ROOT` prefix layout, same fetch script) before the first external release — the conda build's STEP closure (47 DLLs / 56 MB / 16 licences) is acceptable for dev and CI, not for shipping.** See §Recommendation at the end for the evidence and the three risks.

## Log

### 23:44 (08-19) — setup

- Tools present: rustc 1.97.1 (MSVC), cargo 1.97.1, Python 3.10.6 (no
  `zstandard` module yet), curl 8.8.0 (mingw), cmake (VS Build Tools),
  `gh` 2.93.0.
- Download cache dir created at `%LOCALAPPDATA%\cicada-occt\`.

### 23:45 (08-19) — downloads (recorded exactly)

| What | Value |
|---|---|
| Package | conda-forge `occt` 7.8.1, build `novtk_hdfbec02_103` (latest novtk build of 7.8.1 at probe time), subdir `win-64` |
| URL | `https://api.anaconda.org/download/conda-forge/occt/7.8.1/win-64/occt-7.8.1-novtk_hdfbec02_103.conda` |
| Size | 24,745,172 bytes |
| sha256 | `28e3da6feaec750f1a7b64f0aebc7a5f209d64ce3cbc49a3d1a22d2c81ff32d1` (matches the anaconda.org file listing) |
| Listing used | `https://api.anaconda.org/package/conda-forge/occt/files` saved as `occt_files.json` (571 files across all versions) |
| Extraction | `.conda` = zip of `.tar.zst` parts (`pkg-*.tar.zst`, `info-*.tar.zst`); `pip install zstandard` (0.25.0) + a 20-line `extract_conda.py` (kept in the cache dir) |
| Extracted to | `%LOCALAPPDATA%\cicada-occt\occt-7.8.1-win64\` — conda's Windows layout puts the prefix under `Library\` (`Library\include\opencascade` 8,153 headers / 48 MB, `Library\lib` 51 import `.lib` 27 MB, `Library\bin` 50 `TK*.dll` + `ExpToCasExe.exe` 53 MB, `Library\lib\cmake\opencascade\*.cmake`) |
| `info/index.json` depends | `fontconfig >=2.14.2`, `fonts-conda-ecosystem`, `freeimage >=3.18.0`, `freetype >=2.12.1`, `rapidjson`, `ucrt >=10.0.20348.0`, `vc >=14.2`, `vc14_runtime >=14.29.30139`; license `LGPL-2.1-only` (`info/licenses/LICENSE_LGPL_21.txt`) |
| Package config | `OpenCASCADE_BUILD_SHARED_LIBS ON`, `BUILD_TYPE Release`, compiler `vc14`, `/MD`, WITH_FREETYPE ON, WITH_FREEIMAGE ON, WITH_TBB OFF, WITH_VTK OFF, WITH_TCL OFF |

In the first session none of the conda dependency packages were
downloaded: the modeling libraries the probe links do not import
freetype/freeimage (verified with `dumpbin /dependents`). **The review
revision (03:15) fetched the runtime closure the STEP path needs** —
17 packages in all, every one recorded below (URL pattern
`https://api.anaconda.org/download/conda-forge/<name>/<version>/win-64/<file>`,
sizes and sha256 verified against the anaconda.org listing by
`tools/probes/occt-2026-08/fetch_conda_pkg.py`, extracted under
`%LOCALAPPDATA%\cicada-occt\deps\<file-without-.conda>\`;
the listings `<name>_files.json` and a `manifest.tsv` sit beside them).
Builds were chosen to match the occt `_103` build's era and VC-runtime
floor where the constraint allowed; a conda solver would pick newer
builds of the same versions — the DLL *names* (what the loader needs)
are the same:

| package | file (win-64) | bytes | sha256 | licence (conda `index.json`) |
|---|---|---|---|---|
| occt 7.8.1 | `occt-7.8.1-novtk_hdfbec02_103.conda` | 24,745,172 | `28e3da6feaec750f1a7b64f0aebc7a5f209d64ce3cbc49a3d1a22d2c81ff32d1` | LGPL-2.1-only |
| freetype 2.13.3 | `freetype-2.13.3-h0b5ce68_0.conda` | 510,718 | `67e3af0fbe6c25f5ab1af9a3d3000464c5e88a8a0b4b06602f4a5243a8a1fd42` | GPL-2.0-only OR FTL |
| freeimage 3.18.0 | `freeimage-3.18.0-h8310ca0_22.conda` | 465,887 | `89ff5bd00c94d201b76f90b939cbd9ec013171c45d9967f7dac71d330cd10343` | GPL-2.0-or-later OR GPL-3.0-or-later OR FreeImage |
| imath 3.1.12 | `imath-3.1.12-hbb528cf_0.conda` | 160,408 | `184c796615cebaa73246f351144f164ee7b61ea809e4ba3c5d98fa9ca333e058` | BSD-3-Clause |
| libjpeg-turbo 3.0.0 | `libjpeg-turbo-3.0.0-hcfcfb64_1.conda` | 822,966 | `4e7808e3098b4b4ed7e287f63bb24f9045cc4d95bfd39f0db870fc2837d74dff` | IJG AND BSD-3-Clause AND Zlib |
| libpng 1.6.44 | `libpng-1.6.44-h3ca93ac_0.conda` | 348,933 | `0d3d6ff9225f6918ac225e3839c0d91e5af1da08a4ebf59cac1bfd86018db945` | zlib-acknowledgement |
| libraw 0.21.3 | `libraw-0.21.3-h0f5434b_0.conda` | 489,267 | `0d2610b684cd8712bdcf0873b17b1fa3d7ce1105550264b7fd91b184a00d1a28` | LGPL-2.1-only |
| libtiff 4.7.0 | `libtiff-4.7.0-h550210a_6.conda` | 983,988 | `fd27821c8cfc425826f13760c3263d7b3b997c5372234cefa1586ff384dcc989` | HPND |
| libwebp-base 1.4.0 | `libwebp-base-1.4.0-hcfcfb64_0.conda` | 274,359 | `d0ca51cb1de9192be9a3238e71fbcca5a535619c499c4f4c9b2ed41c14d36770` | BSD-3-Clause |
| libzlib 1.3.1 | `libzlib-1.3.1-h2466b09_2.conda` | 55,476 | `ba945c6493449bed0e6e29883c4943817f7c79cbff52b83360f7b341277c6402` | Zlib |
| openexr 3.3.2 | `openexr-3.3.2-h3924f79_1.conda` | 1,179,194 | `78064a2ffa02d3a23f27f63f04ac0ab9ad6ab95f61b2fe5e67ba883366694930` | BSD-3-Clause |
| openjpeg 2.5.3 | `openjpeg-2.5.3-h24db6dd_1.conda` | 245,076 | `c29cb1641bc5cfc2197e9b7b436f34142be4766dd2430a937b48b7474935aa55` | BSD-2-Clause |
| libdeflate 1.23 | `libdeflate-1.23-h76ddb4d_0.conda` | 155,548 | `881244050587dc658078ee45dfc792ecb458bbb1fdc861da67948d747b117dc2` | MIT |
| lcms2 2.16 | `lcms2-2.16-h67d730c_0.conda` | 507,632 | `f9fd9e80e46358a57d9bb97b1e37a03da4022143b019aa3c4476d8a7795de290` | MIT |
| lerc 4.0.0 | `lerc-4.0.0-h6470a55_1.conda` | 164,701 | `868a3dff758cc676fa1286d3f36c3e0101cca56730f7be531ab84dc91ec58e9d` | Apache-2.0 |
| liblzma 5.8.1 | `liblzma-5.8.1-h2466b09_2.conda` | 104,935 | `55764956eb9179b98de7cc0e55696f2eff8f7b83fc3ebff5e696ca358bca28cc` | 0BSD |
| zstd 1.5.7 | `zstd-1.5.7-h534d264_7.conda` | 387,535 | `ca7daae4f218a11fab82cc2857f0ea518ec3f46acec60490485347a4c22c6b3e` | BSD-3-Clause |

(Also downloaded and set aside unused: `libzlib-1.3.1-hfb9b0fc_ng_233_1.conda`,
92,385 B, sha256 `34f5182a…`, a zlib-ng variant picked first by "newest
upload"; replaced by the plain build above. `jxrlib`, `fontconfig`,
`fonts-conda-ecosystem`, `rapidjson` are in the metadata `depends` but
no DLL in the closure imports them, so they were not fetched.) The
dependency set was found by iterating `dll_closure.py` until
`MISSING: none` — three rounds: freetype + freeimage; then the nine
codecs FreeImage imports; then the five those import (`deflate.dll`,
`lcms2.dll`, `Lerc.dll`, `liblzma.dll`, `zstd.dll`). Two of the
"newest build of this version" picks carry constraints a real solve
would have to reconcile (`libtiff _6` wants `libdeflate ≥1.24`,
`openexr _1` wants `<1.24`); irrelevant for loading, relevant for the
WP-A fetch table, which should come from one recorded `conda-lock`
solve rather than from this hand-picked set.

### 00:20–00:55 — Q1: prebuilt build/link on Windows (MSVC)

**Binding.** `opencascade` / `opencascade-sys` from
`https://github.com/bschwind/opencascade-rs` at
`d114250a464d50cde1901743d3bf523a099d6887` (2026-08-03, HEAD of `main`
at probe time); crates.io carries only stale 0.2.0 releases. The probe
depends on it as `{ git, rev, default-features = false }` — the
`builtin` feature (source build of the OCCT 7.8.1 submodule via
`occt-sys`) is off, so `opencascade-sys/build.rs` runs its tiny cmake
project (`OCCT/CMakeLists.txt`: `find_package(OpenCASCADE REQUIRED)`,
writes `occ_info.txt`) with `register_dep("occt")`, which puts
`DEP_OCCT_ROOT` on `CMAKE_PREFIX_PATH`, then checks the version. **The
check is looser than "7.8.x"**: `build.rs` has `OCCT_VERSION = (7, 8)`
and rejects only `major != 7 || minor < 8` — a conda `occt 7.9.x` prefix
(7.9.0–7.9.3 exist in the saved listing) would be accepted silently and
move every golden hash. The exact-version pin therefore belongs in
`tools/fetch_occt.py` (assert the `_103` file sha256s of §4c) and in the
CI job, not in the binding.

**`DEP_OCCT_ROOT`** = `%LOCALAPPDATA%\cicada-occt\occt-7.8.1-win64\Library`
(the dir holding `include/`, `lib/`, `bin/`, `lib/cmake/opencascade`).
Detection worked first try: cmake found `OpenCASCADEConfig.cmake`,
reported 7.8.1, `BUILD_SHARED_LIBS=ON` → the build script emits
`rustc-link-lib=dylib=TK…` for its 24 libs plus `user32`. Two harmless
cmake warnings (a `cmake_minimum_required(3.1)` deprecation; a syntax
warning in OCCT's own config file at `/wd"26812"`).

**First build: FAILED in the C++ glue (not in OCCT, not at link).**
`cargo build --release` → cc-rs compiling the 41 cxx bridge TUs, the
first failing TU (`b_rep.rs.cc`) with three `error C2440`. To get the
complete list at once I compiled all 41 TUs by hand with the same
flags (`compile_all.bat`, 19 s wall): **22 error sites in 10 TUs, one
root cause**, exactly the one PR #230 describes:

- On MSVC, OCCT's `Standard_Handle.hxx` (line 416 ff.) defines
  `Handle_X` via `DEFINE_STANDARD_HANDLECLASS` as a **class derived
  from** `opencascade::handle<X>` ("to allow exporting this type in
  C++/CLI"); on every other compiler it is a plain `typedef`. The
  binding declares its Rust-visible handle types under the names
  `Handle_X` and allocates `new opencascade::handle<X>(…)` into
  `std::unique_ptr<Handle_X>` — distinct types on MSVC.
- 17 allocation sites (`<function-style-cast>` C2440) in 7 headers:
  `b_rep.hxx` (3), `gc.hxx` (3), `geom.hxx` (5), `geom2d.hxx` (4),
  `geom_api.hxx` (1), `top_tools.hxx` (1) — plus `poly.hxx`, `law.hxx`,
  `t_col_gp.hxx` which already allocate `new Handle_X(ptr)` and compile.
- 5 signature mismatches in cxx-GENERATED code (`'initializing': cannot
  convert from 'overloaded-function'`): cxx assigns each bound function
  to a pointer of the exact declared signature, and `Handle_X` flows
  straight into OCCT APIs —
  `BRepLib_ToolTriangulatedShape::ComputeNormals(…, const Handle(Poly_Triangulation)&)`,
  `BRepOffsetAPI_MakePipeShell::SetLaw(…, const Handle(Law_Function)&, …)`,
  `ShapeAnalysis_FreeBounds::ConnectEdgesToWires(Handle(TopTools_HSequenceOfShape)&, …)`
  and the generic `handle_try_deref(const opencascade::handle<T>&)`
  (`poly.rs.cc`, `top_tools.rs.cc`). These five prove the cheap fix
  ("allocate `new Handle_X` instead") is insufficient: the C++ name the
  Rust side binds to must *be* `opencascade::handle<X>`.
- Nothing else fails: all other 31 TUs compile clean with `/W4`; the
  cmake detection, the include paths, and `/MD` vs the conda `/MD`
  libs all agree.

**Upstream PRs read as starting points.**
- **#230 "Fix MSVC handle name collisions"** (akalari, opened
  2026-08-19, **closed unmerged 2026-08-20 05:49 UTC, fork deleted, no
  discussion** — the `.diff` is still served by GitHub): 26 files,
  +147/−58. Mechanism: `bindings_common.hxx` forward-declares the 16
  wrapper-owned classes and adds `using RustHandle_X = opencascade::handle<X>;`
  for each; every glue header signature uses `RustHandle_X`; every
  Rust bridge `type Handle_X` gets `#[cxx_name = "RustHandle_X"]` (32
  attributes across 14 `.rs` files). Rust-side names are unchanged, so
  the high-level `opencascade` crate needs no edits. Plus a Windows CI
  job (`cargo check -p opencascade` with the bundled OCCT,
  `CMAKE_POLICY_VERSION_MINIMUM=3.5`).
- **#216 "Fix Windows build: CMake profile matching and missing system
  libraries"** (TonyMarkham, open since 2025-10-29): 2 files, +9/−1 —
  (a) `occt-sys` passes Cargo's `PROFILE` to cmake instead of hardcoding
  Release (a `builtin`-only problem: debug `cargo build` looked for
  `TKernel.pdb`); (b) links `advapi32`, `gdi32`, `shell32`, `comdlg32`
  on Windows. **Neither part applies to the prebuilt SHARED path**: the
  import libs resolve the Win32 symbols inside the DLLs (the probe
  linked with only the existing `user32`, no unresolved externals), and
  there is no cmake profile to match because we build nothing. Both
  parts WOULD apply to an own static prebuilt (PR #230's author hit the
  same `SetFileSecurityW`/`AddAce` unresolved externals in the bundled
  static build).

**Minimal patch needed and applied (every line recorded).** The
source-only part of #230 — its 25 source files, +131/−58, with the
workflow hunk dropped — is the minimum that compiles: the five
generated-code mismatches force the alias approach, and the alias
approach forces every header signature and every bridge type to
follow. I audited the diff line by line: besides `Handle_` →
`RustHandle_` renames it contains only the 16 forward declarations, the
16 `using` aliases, `#include <Standard_Handle.hxx>`, one
single-line → two-line reflow of `DynamicType` (clang-format), and
whitespace re-alignment of wrapped parameter lists. The exact diff is
committed as `tools/probes/occt-2026-08/patches/0001-msvc-handle-aliases.diff`
and was applied with `git apply` (clean) to a scratch clone at the
pinned rev (`%LOCALAPPDATA%\cicada-occt\opencascade-rs-patched`
— the probe's download-cache dir rather than `%TEMP%`, so the
`[patch]` path in the committed `Cargo.toml` survives reboots). The
probe `Cargo.toml` carries
`[patch."https://github.com/bschwind/opencascade-rs"] opencascade = { path = … }`.
No other change to the binding was needed — no build.rs edit, no
link-lib additions, no cmake flags, no `CMAKE_POLICY_VERSION_MINIMUM`
(this machine's cmake accepted `cmake_minimum_required(3.1)` with a
deprecation warning; a cmake ≥ 4.0 host would need
`CMAKE_POLICY_VERSION_MINIMUM=3.5` in the environment, as #230's CI
job sets — worth one line in the build script of an own fork).

**Second build: compiles, links, runs.** `cargo build --release` after
the patch: `Finished release … in 25.17s` (dependencies already built by
the failed run; the 41 glue TUs + link dominate). The binary:
`occt-probe.exe` **317,952 bytes** — everything OCCT stays in the DLLs.
`occt-probe smoke`:

```
box         faces=6   edges=24  vertices=24     triangles=12     shape_type=Solid
extrude     faces=6   edges=24  vertices=24     triangles=12     shape_type=Solid
difference  faces=10  edges=48  vertices=48     triangles=32     shape_type=Compound
loft        faces=6   edges=24  vertices=584    triangles=1132   shape_type=Solid
smoke OK
```

(`edges` counts per-face incidences — the binding's `TopExp_Explorer`
iterator visits shared edges once per face. `BRepAlgoAPI_Cut` returns a
`Compound` wrapping one solid — the `Solid` kind's constructor will
have to unwrap/validate that. The loft between two rectangles gives
B-spline faces, hence 1,132 triangles at deflection 0.01.)

**Shared vs static; DLL count; PATH.** The conda package is
**shared-only** (`BUILD_SHARED_LIBS ON`; the `.lib`s are import libs).
`dumpbin /dependents` on the probe exe and transitively on the DLLs:

- The linker kept only the **8** TK import libs actually referenced
  (`TKernel TKMath TKBRep TKTopAlgo TKPrim TKMesh TKBO TKOffset`) of the
  24 the build script lists; transitively the process needs **15 OCCT
  DLLs, 30.0 MB**: `TKBO TKBRep TKBool TKFillet TKG2d TKG3d TKGeomAlgo
  TKGeomBase TKMath TKMesh TKOffset TKPrim TKShHealing TKTopAlgo TKernel`.
  Non-OCCT imports: `KERNEL32 ADVAPI32 USER32 WSOCK32`, the UCRT
  api-sets (in the OS since Windows 10), and `MSVCP140 VCRUNTIME140
  VCRUNTIME140_1` — the VC++ 2015–2022 redistributable. A Rust MSVC
  binary already depends on `VCRUNTIME140` + UCRT; `MSVCP140.dll` (the
  C++ standard library) is the one genuinely new runtime requirement.
  **No `freetype.dll` / `FreeImage.dll`** for this set.
- **STEP (corrected 2026-08-20 — the first version of this bullet
  hand-walked the chain as `TKCAF → TKV3d` and called "whether STEP can
  avoid CAF" an open question; both wrong).** Measured with
  `tools/probes/occt-2026-08/dll_closure.py` (a pure-Python PE import
  walker that descends only into DLLs found in the given dirs; its
  numbers for the probe exe reproduce the `dumpbin` ones above
  exactly):
  - `TKCAF` imports `TKLCAF TKBO TKTopAlgo TKBRep TKGeomBase TKG3d
    TKMath TKernel` — **no** `TKV3d`. The visualization edge is
    **`TKDESTEP → TKXCAF → TKV3d → TKService`**, and `TKService`
    hard-imports **`freetype.dll`, `FreeImage.dll`, `WINMM.dll`**
    (plain imports, not delay-load — the loader resolves them before
    `main` runs). `TKDESTEP` alone (`STEPControl_Reader/Writer`) has a
    closure of **23 TK DLLs, 36,697,088 B** + those three; the conda
    package is built with `USE_FREETYPE` (CMake default ON) and
    `-D USE_FREEIMAGE:BOOL=ON` (`info/recipe/bld.bat`), so **STEP
    cannot avoid the font/image libraries in conda's build**, no matter
    how the fork trims its link list. `occt-sys` (the `builtin` source
    path) configures `USE_FREETYPE=FALSE USE_FREEIMAGE=FALSE` — an
    own-built OCCT has no such edge; that is why the static probe exe
    below imports only system DLLs.
  - Modeling set (the 8 libs the probe references) + `TKDESTEP`:
    **26 TK DLLs, 44,790,784 B**. The binding's full 24-lib list: **29
    TK DLLs, 49,017,344 B**. Both + `freetype.dll` + `FreeImage.dll` +
    `WINMM.dll` (system).
  - `FreeImage.dll` from conda-forge is itself built against **shared
    codecs**: it imports `Imath Iex OpenEXR jpeg8 libpng16 libwebp
    libwebpmux openjp2 raw tiff zlib`, which import `deflate lcms2 Lerc
    liblzma zstd` (and `IlmThread OpenEXRCore libsharpyuv`). Resolving
    everything (§setup table, 16 packages): **21 non-OCCT DLLs,
    11,101,184 B**, plus `VCOMP140.DLL` (the OpenMP runtime — a second
    VC++ redistributable a Rust binary does not otherwise need).
  - **Runtime proof**, second probe binary `occt-probe-step`
    (`src/bin/step.rs`: `Shape::write_step` + `read_step` on the box —
    its link set therefore includes `TKDESTEP`; 243,200 B), launched
    via `Process.Start` with an explicit `PATH`, cwd `C:\Windows\Temp`:
    `PATH = System32;<pkg>\Library\bin` → **exit `0xC0000135`**;
    `+ freetype + freeimage bin dirs only` → still `0xC0000135` (the
    codecs); `+ all 16 dependency bin dirs` → **exit 0**, `box.step`
    written (15,416 B, 350 entities) and read back (6 faces, `Solid`).
    Bundle form: exe + exactly the 47 DLLs `dll_closure.py` lists
    copied beside it (`bundle-test-step\`, **56,135,168 B** on disk),
    `PATH = System32;Windows` only → exit 0. Two WP-C notes fell out:
    the STEP writer prints its transfer statistics to **stdout**
    (`Message_Printer` must be redirected in the seam — cicada-server's
    stdout is not a log), and `FILE_NAME(...,'2026-08-20T03:25:38',...)`
    is the run-dependent header already planned for normalization.
  - So the shipping budget through conda's build is **15 DLLs / 30.0 MB
    (modeling only) or 47 DLLs / 56.1 MB (with STEP)**, the latter
    carrying 16 third-party licences (§4a). A `cicada-geom` sub-feature
    `occt-step` still isolates the 15-DLL closure for builds without
    STEP — that is the part of the earlier framing that survives; "STEP
    avoids freetype" does not.
- **PATH at runtime: yes, required.** Launching the exe with a clean
  environment (`Process.Start`, cwd elsewhere) exits with
  `0xC0000135 STATUS_DLL_NOT_FOUND`; the msys shell reports it
  confusingly as `api-ms-win-crt-locale-l1-1-0.dll: cannot open shared
  object file`. It runs with `Library\bin` on `PATH`, **or with the 15
  DLLs copied next to the exe** (verified: `bundle-test/` = exe + 15
  DLLs = 29 MB on disk, launched from another working directory → exit
  0, `smoke OK`). A shipped `cicada.exe` would therefore carry 15 DLLs
  (30.0 MB) beside it without STEP, or **47 DLLs (56.1 MB) with STEP**
  through conda's build (measured above; the earlier "~29 + 3" here was
  the hand-walk's guess). (A false-PASS trap worth recording: my
  first "runs without PATH" observation was because the shell's cwd was
  `Library\bin` — Windows searches the cwd; don't test DLL resolution
  from inside the DLL directory.)

**Q1 verdict: GREEN with one patch; AMBER for shipping STEP through
conda's build.** The prebuilt-shared path works on MSVC today with the
reviewed, mechanical PR-#230 rename carried in an own fork (its upstream
PR was closed without merge, so "wait for upstream" is not a plan). The
amber is the 47-DLL / 16-licence STEP closure — fine for development
and CI, not what one ships (§Recommendation). Linux/macOS are expected to be strictly
easier (the `Handle_X` typedef path; CI-verified in upstream's Linux
job for the builtin build) but are NOT verified by this probe — that is
the CI job of Item 3 WP-A.

### 00:58–01:10 — Q2: determinism across processes

Method: `occt-probe dump <dir>` builds the four probe shapes from
transcendental-free inputs (box 10×20×30 at the origin; rectangle 4×6
from four explicit points extruded 40 along Z; box − prism; loft between
a 10×20 rectangle at z=0 and a 6×12 rectangle at z=30), writes each with
`Shape::write_brep_bin` (BinTools) and `Shape::write_brep_text`
(BRepTools) BEFORE any tessellation, hashes the files, tessellates
(deflection 0.01), hashes positions+indices and normals separately, then
re-reads the BinTools file and re-writes it (round trip). Two separate
processes (`run1`, `run2`, fresh cwd, nothing cached on disk by OCCT),
compared line-by-line and with `cmp` on the raw files.

**Two binding defects surfaced before the question could be answered
honestly** (both now carried as probe patches and both required for
Item 3 WP-B):

1. **`write_brep_bin` was silently the text writer.** First run: the
   "BinTools" and "BRepTools" files were byte-identical (2,565 B) and
   both started with `DBRep_DrawableShape … CASCADE Topology V3`. Root
   cause (verified with `dumpbin /linkermember:1` on `rust-occt.lib`):
   the `bin_tools` and `b_rep_tools` cxx bridges both declare
   `fn write` / `fn read` in the root namespace with different
   `#[cxx_name]`s, but cxx mangles the *Rust* name —
   `cxxbridge1$199$write` / `$read` — so two archive members define the
   same symbols; the archive index points both at `b_rep_tools.rs.o`,
   `bin_tools.rs.o` has no unique symbol and is never pulled, and no
   toolchain warns (the same archive-member logic holds for GNU ld, so
   this is not Windows-specific). `Shape::write_brep_bin` /
   `read_brep_bin` in the shipped binding therefore read and write the
   ASCII format. Fix: `patches/0002-bin-tools-symbol-collision.diff`
   (4 lines: rename the bridge fns to `write_bin`/`read_bin`, update the
   two call sites in `shape.rs`). Worth an upstream issue; the fork
   carries it regardless. **Review revision:** `dump` now asserts the
   `.bin` file starts with `\nOpen CASCADE Topology V4` and the `.brep`
   with `DBRep_DrawableShape` and panics otherwise — with only patch
   0001 applied the probe used to exit 0 printing `bin == text` hashes
   (reproduced by the reviewer), a false PASS for a format it never
   exercised. The numbers in the table below were produced WITH 0002
   and re-run after the revision: identical.
2. **OCCT 7.8's path-taking `Write` overloads default to
   `theWithTriangles = TRUE`** (`BinTools.hxx` line 93 ff.: "This alias
   writes shape with triangulation data"; same for `BRepTools::Write`),
   and the binding passes only the path. Measured: writing the same
   shape again AFTER `mesh()` changes the bytes — box 4,494 → 6,931 B,
   extrude 2,303 → 4,347 B, difference 8,821 → 13,747 B, loft 4,790 →
   43,067 B (`identical_to_premesh=false` for all four). A `Solid`
   whose canonical bytes were taken after display tessellation would
   hash differently from the same solid serialized before it. The WP-B
   serializer must call the 5-argument overload with
   `theWithTriangles = false, theWithNormals = false` and a PINNED
   `BinTools_FormatVersion` (not `_CURRENT`, which moves with OCCT
   releases) — a ~6-line glue function in the fork, not an upstream
   question.

**Results with the real BinTools path (two processes):**

| Shape | BinTools V4 bytes | sha256 (prefix) | BRepTools text bytes | mesh verts / tris | identical across processes |
|---|---|---|---|---|---|
| box | 4,494 | `d83ce546…` | 2,565 | 24 / 12 | bin ✓ text ✓ after-mesh ✓ mesh ✓ normals ✓ |
| extrude | 2,303 | `b0265b60…` | 1,623 | 24 / 12 | bin ✓ text ✓ after-mesh ✓ mesh ✓ normals ✓ |
| difference | 8,821 | `d2d1784f…` | 5,237 | 48 / 32 | bin ✓ text ✓ after-mesh ✓ mesh ✓ normals ✓ |
| loft | 4,790 | `711eaf3d…` | 3,001 | 584 / 1,132 | bin ✓ text ✓ after-mesh ✓ mesh ✓ normals ✓ |

- `diff run1.txt run2.txt` is empty; `cmp` on all 12 files (4 shapes ×
  bin / brep / after_mesh.bin) reports identical. Full hashes are in the
  run logs (`%LOCALAPPDATA%\cicada-occt\det\run{1,2}.txt`)
  and reproducible with the committed probe.
- **Round trip is byte-stable**: `BinTools::Read` → `BinTools::Write`
  reproduces the original bytes exactly for all four shapes — the
  format is a fixed point, so "canonical bytes = BinTools V4 of the
  shape" is well defined and re-hydrating from the store will not
  drift the hash.
- **Nothing run-dependent in either writer.** BinTools V4 header:
  `\nOpen CASCADE Topology V4, (c) Open Cascade\n` then `Locations`,
  `Curve2ds`, …; BRepTools: `DBRep_DrawableShape\n\nCASCADE Topology
  V3, (c) Open Cascade\n`. Format-version strings only — no date, no
  build id, no OCCT version. (The STEP writer, by contrast, DOES stamp
  `FILE_NAME(... '2026-08-20T…' ...)` — measured with `occt-probe-step`:
  `FILE_NAME('Open CASCADE Shape Model','2026-08-20T03:25:38',('Author'),(`;
  that is the "header timestamps normalized" line already in Item 3
  WP-C.)
- The binary format is LARGER than the text format for these shapes
  (raw IEEE doubles vs shortest-decimal text of round numbers) — size
  is not an argument either way; hash the binary: it is exact and
  needs no float-printing policy.
- The `difference` result is a `TopoDS_Compound` holding one solid;
  the compound wrapper is part of the serialized bytes, so the `Solid`
  constructor must normalize (unwrap single-solid compounds, or refuse
  multi-solid results loudly) BEFORE hashing, or identical solids
  reached via different operations hash differently.

**What this does and does not decide.** On one machine / one compiler /
one OCCT build, OCCT's B-rep construction and serialization are
byte-deterministic for these inputs, so **`Solid` can hash its canonical
BinTools bytes** (blake3 at construction, the Merkle model unchanged)
rather than a geometric summary — exactly like the Manifold seam.
What remains open is cross-OS/cross-build identity (different libm,
different compiler flags in the three conda builds, FMA contraction):
the golden-hash CI job in Item 3 WP-A decides whether the golden hashes
are per-OS or universal. The fallback if they diverge is already in
docs/17 ("or the determinism policy the probe forced is recorded"):
per-platform golden files, not a summary hash.

### 01:10–01:20 — Q3: timings (release build)

`occt-probe bench <parts>`: `parts` independent parts, each at its own
X offset; per op, wall-clock over all parts then per part. One thread
(the binding exposes no `SetRunParallel`; OCCT's own parallelism is off
by default). Intel i7-13700KF (8P+8E, 24 threads), 64 GB; numbers from the second of
two runs — the first run at each size agreed within 5 %.

| op (per part) | 1 part | 100 parts | 1,000 parts | per part @1,000 |
|---|---|---|---|---|
| `box` (`BRepPrimAPI_MakeBox`) | 0.113 ms | 2.64 ms | 23.5 ms | **0.024 ms** |
| `extrude` (4-pt wire → face → `MakePrism`) | 0.343 ms | 9.95 ms | 95.6 ms | **0.096 ms** |
| `difference` (`BRepAlgoAPI_Cut`, box − prism) | 3.69 ms | 288.5 ms | 2,850 ms | **2.85 ms** |
| `mesh(occt)` (`BRepMesh_IncrementalMesh`, deflection 0.01) | 0.99 ms | 85.7 ms | 850 ms | **0.85 ms** |
| `mesh(extract)` (binding's per-node FFI copy-out) | 0.025 ms | 1.30 ms | 13.4 ms | 0.013 ms |
| `tessellate` = mesh + extract | 1.02 ms | 87.0 ms | 864 ms | **0.86 ms** |
| `loft` (`ThruSections`, two rectangles, solid) | 1.20 ms | 82.5 ms | 839 ms | 0.84 ms |

- **Linear in parts** — no super-linear surprises from 1 to 1,000; the
  1-part numbers carry first-call warm-up (allocator, page-ins), which
  is why per-part at 1 > per-part at 1,000. Single-threaded timings on
  a hybrid CPU also depend on which core the thread lands on; the
  run-to-run spread observed was ≤ 5 %.
- **A simple boolean costs ~3 ms**; a 1,000-part box-minus-prism
  pipeline is 2.85 s of booleans + 0.86 s of tessellation + 0.12 s of
  primitives ≈ **3.8 s single-threaded**. For scale, the wall corpus's
  1,200-part cold carve on the Manifold mesh tier is 6.5 s with the
  rayon fan-out (docs/15). OCCT booleans on independent shapes are
  thread-safe (OCCT's own `BOPAlgo` parallel mode and its documented
  per-thread allocator; `MMGT_OPT` env var) — if the `Solid` nodes run
  under the scheduler's chunked `each()` like every other node, the
  1,000-part figure divides by the core count, but that is an
  assumption to verify in WP-C, not a measurement.
- **Tessellation at 0.01 deflection: 0.85 ms per 32-triangle part**;
  OCCT's mesher dominates, the binding's extraction (one FFI call per
  node, UV, normal and triangle) is 1.5 % — fine as is, no need for a
  bulk copy seam in WP-B. The loft's B-spline faces (1,132 triangles)
  mesh in ~1 ms, so a display-cache miss on a `Solid` is ~1 ms per
  simple solid — inside the 0.5 ms p50 cheap-slider budget only via
  the hash-keyed tessellation cache (cache hit) — slider drags that
  re-run a boolean are ≥3 ms per solid regardless, i.e. B-rep
  pipelines will feel "compute-on-release" (Item 3b) sooner than mesh
  ones.
- **Binary & build**: `occt-probe.exe` **325,632 bytes** at the time of
  this measurement (332,800 B after the review revision added the
  `dump` header asserts and the `throw` subcommand; `occt-probe-step`
  is 243,200 B — everything OCCT lives in the DLLs; the cxx glue static
  lib `rust-occt.lib` is 3.3 MB). **Cold `cargo build --release`** in a fresh target dir
  (crates.io + git checkouts already cached; 24 threads): **28.5 s
  wall**, of which the `opencascade-sys` build script is **23.1 s**
  (≈3 s cmake configure for detection + the 41 C++ glue TUs at `/O2`
  + `lib`), everything else (59 units incl. `syn`, `cxx`, `glam`,
  `sha2`) fits in the remaining 5 s. **Link/relink**: the bin unit is
  0.57 s; touch `main.rs` → rebuilt in 0.72 s wall. Nothing here is
  slow enough to matter for CI or for the inner loop once the glue is
  cached.
- The **source build** (`builtin` feature: `occt-sys` compiles the
  OCCT 7.8.1 submodule statically through cmake) was started detached
  at 01:16 against the same patched clone (submodule fetched shallow,
  252 MB, 33 s); its wall-clock is recorded below when it finishes or
  when this probe hits its cap, whichever first.

### 01:22–01:45 — Q4: policy drafts (NOT applied — DECISIONS.md and deny.toml are untouched by this probe)

#### 4a. License row (draft for DECISIONS.md)

> **OCCT licensing.** OCCT 7.8.1 is `LGPL-2.1-only` **with the Open
> CASCADE Exception 1.0**: object code that incorporates material from
> OCCT header files (inline functions, templates, "CDL generic
> classes") may be distributed "under terms of your choice, provided
> that you give prominent notice in supporting documentation" that it
> uses OCCT. The exception covers exactly what our cxx glue and the
> `Handle<>`/`gp_*` inlines put into *our* object code; it does **not**
> relax LGPL §6 for the OCCT libraries themselves.
> *While private*: nothing is conveyed, nothing is triggered; the only
> obligation is hygiene (keep `LICENSE_LGPL_21.txt` +
> `OCCT_LGPL_EXCEPTION.txt` in the repo's third-party notices, record
> the exact build we link).
> *At go-public, shipping binaries*: **dynamic linking** (the conda
> `TK*.dll` / `libTK*.so` / `libTK*.dylib` beside the executable) is
> LGPL §6(b)'s "suitable shared library mechanism" — obligations are
> the notices, the prominent OCCT acknowledgement, and making the
> *corresponding source of the exact OCCT build* available (for
> conda-forge that is upstream `V7_8_1` + the feedstock's 16 patches at
> feedstock commit `4237a64ca19105111b57b8d18535e9de717d8870`, both
> public — pin that commit next to the package sha256s). **Static
> linking** (an own static prebuilt, or `builtin`) falls under §6(a):
> we would have to provide Cicada's own object files (or another
> relinkable form) so a user can relink against a modified OCCT —
> acceptable only if Cicada itself is open source at that point, and
> the reason static is not the v0.1 default.
> *The Rust binding is LGPL too*: `opencascade`, `opencascade-sys`
> (and the stray `kicad-parser`) are `LGPL-2.1` crates, and Rust links
> them **statically** — so the §6(a) analysis applies to the glue
> regardless of how OCCT itself is linked. If Cicada goes public as a
> proprietary binary, the seam must be Cicada-owned code (the surface
> Item 3 needs is ~25 functions: primitives, prism/loft/revolve/sweep,
> the three booleans, `BRepMesh_IncrementalMesh` + triangulation
> read-out, `BinTools` with explicit flags, STEP read/write) — the OCCT
> exception then covers our object code, and only the OCCT libraries
> stay LGPL. Decide the dependency shape (fork vs. own seam) with this
> in view; it does not block v0.1.
> *If the shipped binary carries conda-forge's STEP closure* (§Q1): the
> 21 extra DLLs bring 16 more notices — FreeType (`FTL` — choose it
> over the GPL-2.0 alternative; requires the FreeType credit), FreeImage
> (choose the **FreeImage Public License**, not the GPL alternatives —
> FIPL permits proprietary use with the notice), LibRaw
> (`LGPL-2.1-only`, dynamic → §6(b), same as OCCT), and permissive
> notices for imath / openexr / libwebp / zstd (BSD-3), openjpeg
> (BSD-2), libjpeg-turbo (IJG + BSD-3 + Zlib), libpng
> (zlib-acknowledgement), libtiff (HPND), libdeflate / lcms2 (MIT),
> lerc (Apache-2.0), liblzma (0BSD), zlib (Zlib). None blocks, all must
> be in the third-party notices; an own-built OCCT with
> `USE_FREETYPE`/`USE_FREEIMAGE` OFF removes all 16 (§Recommendation).

#### 4b. `deny.toml` rows (verified: cargo-deny 0.20.2 against the probe's lock file)

The repo's current `deny.toml` rejects the binding on two counts —
`opencascade` / `opencascade-sys` declare the **deprecated SPDX id
`LGPL-2.1`** (`warning[parse-error]` + `error[rejected]: license is
not explicitly allowed`), and `kicad-parser` (a KiCad PCB importer the
high-level crate drags in for its `kicad` module, 113 lines, unused by
us) has **no license field at all** (`error[unlicensed]`). With the
git dependency unpatched, `[sources] unknown-git = "deny"` adds
**3 × `error[source-not-allowed]`** (one per crate from the repo). The
minimal rows that make `cargo deny check licenses sources` print
`licenses ok, sources ok`:

```toml
# OCCT seam (docs/probes/occt-2026-08.md §Q4). LGPL-2.1 is allowed ONLY
# for the OCCT binding crates; see the DECISIONS.md OCCT-licensing row
# for what that implies at go-public.
[[licenses.exceptions]]
name = "opencascade"
allow = ["LGPL-2.1"]          # "LGPL-2.1-only" once the fork fixes its manifests
[[licenses.exceptions]]
name = "opencascade-sys"
allow = ["LGPL-2.1"]
# kicad-parser carries no license field upstream; the fork should drop the
# dependency (preferred) — until then, clarify + scope it the same way.
[[licenses.clarify]]
name = "kicad-parser"
expression = "LGPL-2.1-only"
license-files = []
[[licenses.exceptions]]
name = "kicad-parser"
allow = ["LGPL-2.1-only"]

# NOTE: add this key to the EXISTING `[sources]` table (deny.toml line 44
# today) — TOML rejects a second `[sources]` header. Shown standalone only
# so the row is readable.
[sources]
allow-git = ["https://github.com/<owner>/opencascade-rs"]   # the fork, not upstream
```

Notes from the runs: the exception must be spelled exactly as the
crate's expression parses (`LGPL-2.1` for the deprecated id; an
`LGPL-2.1-only` exception does NOT match it, and a `clarify` alone
does not exempt a crate — it still needs its `exceptions` row). Two
`parse-error` warnings remain until the fork's manifests say
`LGPL-2.1-only`. The `[graph] all-features = true` already in
`deny.toml` means the nightly cargo-deny job resolves the `occt`
feature's crates without needing OCCT installed (cargo-deny never
builds) — the rows above are checked on every run, not only in the
OCCT job. The `cxx`/`cxx-build`/`cmake`/`link-cplusplus`/`scratch`
transitive crates are all MIT OR Apache-2.0 — no further rows.

#### 4c. CI shape

- **Feature, not default.** `cicada-geom` gains `occt = ["dep:opencascade"]`
  (or `dep:opencascade-sys` if the high-level layer is replaced);
  the seam module, the `Solid` kind's OCCT half and the stdlib nodes
  live under `#[cfg(feature = "occt")]`, propagated as
  `cicada-stdlib/occt`, `cicada-server/occt`, `cicada-cli/occt`.
  Default `cargo check/test/clippy --workspace --all-targets` never
  compiles a line of C++ (the probe's own `[workspace]` table is the
  same idea at project scale). Rule for CI: nothing may run
  `--all-features` outside the OCCT job.
- **The fetch step** (`tools/fetch_occt.py`, Item 3 WP-A): a table of
  the exact files, verified by sha256, extracted with the stdlib
  `zipfile` + `tarfile` plus `zstandard` (pip; 0.25.0 here) into the
  user cache dir (`%LOCALAPPDATA%\cicada-occt\…`, `~/.cache/cicada-occt/…`
  — never the repo), printing the `DEP_OCCT_ROOT` to export (conda's
  layout differs: Windows prefix is `<pkg>\Library`, Unix prefix is
  `<pkg>` itself with `lib/`, `include/opencascade/`, `lib/cmake/opencascade/`).
  The four 7.8.1 `novtk` builds, all build number 103, all uploaded
  2024-10-19, all from the same feedstock commit:

  | subdir | file | bytes | sha256 |
  |---|---|---|---|
  | win-64 | `occt-7.8.1-novtk_hdfbec02_103.conda` | 24,745,172 | `28e3da6feaec750f1a7b64f0aebc7a5f209d64ce3cbc49a3d1a22d2c81ff32d1` |
  | linux-64 | `occt-7.8.1-novtk_he2768ca_103.conda` | 28,405,633 | `801a89aad7276a56117f418ed0ebf1a94fc94b559651898bf679fadef6f98fe9` |
  | osx-64 | `occt-7.8.1-novtk_h6d54593_103.conda` | 24,934,160 | `4a20b71ca27c5a0cfb18e6821e46fce8beef8e659583fc43ea9575e55c3e3fc9` |
  | osx-arm64 | `occt-7.8.1-novtk_hbec2736_103.conda` | 23,594,903 | `f9bd9cb02ab1c48208bec8bed3bc7206ea9595d8dc82964398654cb92216a625` |
  | (linux-aarch64, if ever) | `occt-7.8.1-novtk_h9a07acc_103.conda` | 26,869,574 | `503d87ef4f2dd2b0fe84c2b63bfbaad904cbfc3d3de7762f5ff3fc26cc22652d` |

  URL pattern: `https://api.anaconda.org/download/conda-forge/occt/7.8.1/<subdir>/<file>`
  (also served at `https://conda.anaconda.org/conda-forge/<subdir>/<file>`).
  **The fetch script, not the binding, pins the version**: the
  binding's `build.rs` accepts any 7.x ≥ 7.8 (§Q1), so
  `tools/fetch_occt.py` must assert these exact file sha256s and the
  job must fail if `occ_info.txt` reports anything but 7.8.1.
  Runtime deps: the *modeling* closure needs none of the metadata
  `depends` (verified on win-64) — only the platform C++ runtime (MSVC:
  `MSVCP140`/`VCRUNTIME140`; Linux: the runner's `libstdc++`; macOS:
  `libc++`). **Any binary that links STEP needs the 16 runtime packages
  of the §setup table** (win-64 files, bytes and sha256 there; for
  linux-64 / osx-64 / osx-arm64 the same package names — `freetype
  freeimage imath libjpeg-turbo libpng libraw libtiff libwebp-base
  libzlib openexr openjpeg libdeflate lcms2 lerc liblzma zstd` — whose
  exact files WP-A pins from one recorded `conda-lock` solve per
  platform, the way the win-64 set here came from iterating
  `dll_closure.py` to `MISSING: none`). Extract **all packages into one
  prefix** (that is how conda packages are designed: on Windows every
  DLL then lands in the same `Library\bin`, one loader-path entry; on
  Linux/macOS the `$ORIGIN`/`@rpath`-relative install names resolve
  within the shared `lib/`). Two files in the win-64 package carry
  conda's build-prefix placeholder (`Library/bin/env.bat`,
  `OpenCASCADEVisualizationTargets.cmake`); `find_package` still
  succeeds because the detection links nothing — no prefix rewriting
  needed for our use.
- **The job** (`occt.yml`, matrix `windows-latest` / `ubuntu-latest` /
  `macos-14` (arm64) and, if Intel Macs matter, `macos-13`): checkout →
  `python tools/fetch_occt.py --github-env` (writes `DEP_OCCT_ROOT` and
  the loader path — `PATH` += `Library\bin` on Windows,
  `LD_LIBRARY_PATH` on Linux, `DYLD_LIBRARY_PATH` on macOS — into
  `$GITHUB_ENV`; cache the extracted dir with `actions/cache` keyed by
  the sha256) → `cargo test -p cicada-geom -p cicada-stdlib --features occt`
  → `cargo run -p cicada-cli --features occt -- run examples/07-simple-cad.cic --hashes --cache-dir $RUNNER_TEMP/cache`
  compared against committed golden hashes **per OS** until the three
  agree (then one file). cmake is preinstalled on all three runner
  images; `CMAKE_POLICY_VERSION_MINIMUM=3.5` in the job env guards the
  binding's `cmake_minimum_required(3.1)` against a cmake-4 runner.
  The loader-path step is what the probe showed is non-negotiable
  (exit `0xC0000135` without it — and, once any test binary links
  `TKDESTEP`, equally without the 16 runtime packages: the
  `cargo test … --features occt` step on `windows-latest` would die at
  load time, not at compile time, which is why the fetch step extracts
  the whole set into the one prefix from day one). For release
  packaging the DLLs are copied beside the exe instead
  (`dll_closure.py --json` lists exactly which — that is the packaging
  step's input, not a hand-maintained list).
- **Cold CI minutes (estimate from the measured numbers).** Download
  25–28 MB + extraction: < 30 s. The glue: 23.1 s wall on this
  24-thread desktop ≈ 41 TUs × ~10 s CPU ≈ 7 CPU-minutes → **~2 min on
  a 4-vCPU runner** (Windows runners are the slowest; GitHub's
  macos-14 has 3 cores), cached by `rust-cache` when the fork rev and
  `DEP_OCCT_ROOT` do not change. Everything else is the existing
  workspace build. **Budget ≈ +3 min cold, ≈ +30 s warm, per OS.**
  For the alternative (own prebuilt built from source, once per OCCT
  bump): **unoptimized, 4 min here** (248 s wall for the reduced module
  set through cmake-rs/MSBuild — but that kernel is the 2–2.8×-slower
  one nobody would ship, see the flag trap below); **optimized
  (`/O2`), hour-class per OS** — the `/O2` build here hung and was
  killed after 41 min with 21 of 47 toolkits done (01:47 entry), and
  conda-forge's own CI for this recipe is in the same class. Budget the
  hour, run it once per OCCT bump in a dedicated workflow with plain
  cmake + Ninja, cache the artifact forever after.

#### 4d. Ledger rows forced (drafts — NOT applied; DECISIONS.md is untouched by this probe)

docs/17 §Item P's deliverable is the memo "plus the ledger rows it
forces". Row 16 delegates two things to this probe ("the week-1 probe
decides the fork and the CI shape") and Q2's stated purpose is to
decide canonical-bytes vs summary hashing. Paste-ready rows, in the
ledger's `| Date | Decision | Rationale |` shape; the first two are
revisions of existing rows (append to the row text, as the 2026-08-19
revisions did), the last two are new rows:

> **Row 16 (Rent all geometry kernels), append:** `… (probe
> docs/probes/occt-2026-08.md, 2026-08-20: the binding is an **own fork
> of opencascade-rs at d114250** carrying exactly the reviewed patches
> 0001 MSVC handle aliases (PR #230's source part; upstream closed it
> unmerged), 0002 BinTools symbol collision, 0003 Windows static system
> libs (PR #216's hunk), plus the fork-side additions WP-B needs
> (explicit-flag BinTools writer with a pinned format version,
> `kicad-parser` dropped, `LGPL-2.1-only` manifests); the fork is a
> scaffold — the seam Cicada depends on is to become Cicada-owned code
> (~25 functions) before any proprietary distribution, because the
> binding crates are LGPL and link statically. Prebuilt OCCT =
> conda-forge 7.8.1 build `_103` per platform (win-64 / linux-64 /
> osx-64 / osx-arm64 files and sha256s in the probe §4c), fetched by
> `tools/fetch_occt.py` into the user cache dir as ONE conda prefix
> together with the 16 runtime packages the STEP toolkits need in
> conda's build; the script, not the binding, asserts the exact version.
> The shipped binary uses an own CI-built OCCT artifact with
> `USE_FREETYPE`/`USE_FREEIMAGE` OFF (same prefix layout, same fetch
> script) before the first external release.)` | *Rationale append:*
> `the probe measured the conda build's STEP load set at 47 DLLs / 56 MB
> / 16 licences vs 15 DLLs / 30 MB for modeling alone; acceptable for
> dev + CI, not for shipping; an own artifact removes the 21 codec DLLs`

> **Row 42 (Analytic + B-rep first), append:** `… (2026-08-20, probe
> Q2: `Solid` hashes its **canonical BinTools V4 bytes** — written with
> `theWithTriangles = false, theWithNormals = false` and a PINNED
> `BinTools_FormatVersion` (never `_CURRENT`), after normalizing
> single-solid `Compound` results and refusing multi-solid ones loudly —
> blake3 at construction like every other value; no geometric-summary
> hash. Display tessellation never feeds back into the bytes (the
> default path overloads would embed triangulation).)` | *Rationale
> append:* `byte identity held across processes and across two
> independently compiled OCCT 7.8.1 builds for box / extrude / boolean /
> loft; round trip is a fixed point; the format header carries no
> run-dependent bytes`

> **New row — OCCT licensing:** the §4a text above, verbatim, as one
> row dated 2026-08-20 | `LGPL-2.1 + the Open CASCADE exception; dynamic
> linking keeps OCCT under §6(b); the statically linked LGPL glue is the
> real exposure at go-public; conda's STEP closure adds 16 notices`

> **New row — OCCT golden hashes are per-OS until proven universal:**
> `Golden hashes for OCCT-backed values are committed PER PLATFORM
> (win-64 / linux-64 / osx-arm64 files) and collapse to one file only
> when the dedicated CI job shows all three agree for the
> transcendental-free corpus; a disagreement is a documented per-OS
> golden, never a looser comparison.` | `the probe proved determinism on
> one machine and across two compilers on it, not across libm/FMA
> differences between OSes; docs/17 already names this fallback`

### 01:16–01:35 — source build (`builtin`), for the record

Started detached against the same patched clone with the default
features. **Correction made at 01:50**: `opencascade-sys` declares
`occt-sys = { version = "0.6", optional = true }` — a *crates.io*
dependency, not a path into the repo — so the first three source
builds compiled the OCCT tree packaged inside `occt-sys 0.6.0` from
the registry (124 MB crate), not the submodule I had fetched
(`bd2a789f` = "Update version to 7.8.1", shallow, 252 MB, 33 s —
unnecessary). The two trees are both OCCT 7.8.1 and differ in **zero**
C++ source files (`diff -rq` over `src/`: 41 non-code files differ by
line endings, the registry crate omits `OpenGl`/`QABugs`/`ViewerTest`),
so every conclusion below holds for "pristine upstream 7.8.1"; only
the routing note for patch 0004 changes (it lands in
`crates/occt-sys/src/lib.rs` of the fork and is compiled only when
`occt-sys` is taken from the fork via `[patch.crates-io]`, which the
fourth build below does).

- **Wall-clock: 262.6 s total, 248.1 s of it the `opencascade-sys`
  build script** (cmake configure + the static compile of the reduced
  module set — 47 `TK*.lib`, 818 MB — + the glue), 24 threads. Then it
  **failed at the final link with 29 `LNK2019/LNK2001` for 23
  `advapi32` imports** (`RegOpenKeyW`, `OpenProcessToken`, `EqualSid`,
  `GetUserNameW`, `SetFileSecurityW`, `AddAce`, `GetAce`, …): exactly
  PR #216's second hunk, exactly the failure PR #230's author reports.
  So: **#216's link-lib lines are required on the static path and
  harmless on the shared one**; applied as
  `patches/0003-windows-static-system-libs.diff` (7 lines in
  `build.rs`, commented). Relink after the patch: 2 m 15 s (the build
  script re-ran: cmake no-op + glue recompile + a 31 MB link).
- **Static result: `occt-probe-builtin.exe` = 31,144,960 bytes, zero
  OCCT DLLs**; runs from a clean `Process.Start` in another directory;
  imports only `KERNEL32 USER32 ADVAPI32 WS2_32 ntdll` + the UCRT
  api-sets + `MSVCP140 VCRUNTIME140 VCRUNTIME140_1` (fully
  self-contained would additionally need OCCT built `/MT` and
  `+crt-static`). No freetype/FreeImage anywhere: `occt-sys` configures
  `USE_FREETYPE=FALSE USE_FREEIMAGE=FALSE` (and `USE_RAPIDJSON`,
  `USE_TBB`, `USE_OPENGL`, … all FALSE; `BUILD_MODULE_Draw` and
  `ApplicationFramework` off) — the same switches an own *shared*
  prebuilt would use to ship STEP without the 21-DLL codec closure
  (§Q1).
- **Cross-build determinism (bonus for Q2):** the static probe's
  `dump` output is byte-identical to the conda-shared probe's for
  every line — BinTools V4, BRepTools text, after-mesh, positions +
  indices, normals, all four shapes. Two different OCCT builds
  (pristine upstream 7.8.1 compiled by MSVC 14.44 at the flags below
  vs conda-forge's 16-patch build compiled by MSVC 19.29 `/O2
  /fp:precise`), identical bytes. Same OS and CPU, so still not the
  cross-OS answer, but it removes "compiler/build flags" from the list
  of things likely to break the golden hashes.
- **The flag trap.** The static build was **2–2.8× slower** on every
  op (1,000 parts: difference 7.42 ms vs 2.97 ms per part, mesh 1.73 vs
  0.87, loft 2.36 vs 0.88). Cause, from `OCCT/build/CMakeCache.txt`:
  `CMAKE_CXX_FLAGS_RELEASE = -nologo -MD -Brepro -W0` — the `cmake`
  crate replaces CMake's MSVC default (`/MD /O2 /Ob2 /DNDEBUG`) with
  its own flags, and upstream's `[profile.release.package.occt-sys]
  opt-level = 3` does not put `/O2` back. **Upstream's `builtin`
  feature ships an unoptimized OCCT on MSVC.** The fix for any own
  source-built artifact is explicit: `CFLAGS`/`CXXFLAGS` =
  `/O2 /Ob2 /DNDEBUG` (or `.cxxflag(...)` / a `CMAKE_CXX_FLAGS_RELEASE`
  define in `occt-sys`). Two rebuilds chased this down: (a) with
  `CFLAGS`/`CXXFLAGS = /O2 /Ob2 /DNDEBUG` in the environment (264 s):
  `/DNDEBUG` survived, `/O2 /Ob2` did NOT — `cmake-rs` filters every
  `-O`/`/O` argument from the compiler flags it forwards
  (`skip_arg` in `cmake-0.1.58/src/lib.rs`, "let cmake deal with
  optimization/debuginfo") and then defines `CMAKE_CXX_FLAGS_RELEASE`
  anyway, so the env route cannot work on MSVC; timings unchanged
  (difference 6.96 ms, mesh 1.67 ms, loft 2.25 ms per part), hashes
  still identical to conda's. (b) with
  `.define("CMAKE_{C,CXX}_FLAGS_RELEASE", "/MD /O2 /Ob2 /DNDEBUG")` in
  `occt-sys` (`patches/0004-occt-sys-msvc-release-flags.diff`, routed
  in through `[patch.crates-io]`): `CMakeCache.txt` now reads
  `CMAKE_CXX_FLAGS_RELEASE:STRING=/MD /O2 /Ob2 /DNDEBUG` — the define
  wins because `cmake-rs` only sets the variable when the crate has
  not (`!self.defined(...)`). Its numbers follow.

- **Optimized source build (patch 0004 in effect), started 01:47 —
  HUNG, killed at 02:28.** `CMakeCache.txt` confirmed
  `CMAKE_CXX_FLAGS_RELEASE=/MD /O2 /Ob2 /DNDEBUG` (the define route
  works), the Visual Studio generator produced 21 of 47 static
  toolkits, and then the tree deadlocked: at 02:14 and again at 02:27,
  89 `cl.exe` + 24 `MSBuild.exe` alive, **3 % total CPU**, each
  compiler holding 12–19 s of CPU time since 01:47:5x, no `link.exe`,
  no `mspdbsrv.exe`. Not diagnosed (MSBuild node/`/MP`
  oversubscription or node reuse across the earlier builds are the
  usual suspects) — the three unoptimized builds through the same
  cmake-rs + MSBuild path completed in 245–264 s each, so it is not a
  deterministic failure of the route. Consequences for the memo: (a)
  there is **no like-for-like `/O2` static timing**; the only fair
  statement is that conda's `/O2` build is the optimized reference and
  the unoptimized static build is 2–2.8× slower than it; (b) an own
  source-built artifact pipeline should drive OCCT with plain
  cmake + Ninja in a dedicated job, not through cmake-rs/MSBuild inside
  a cargo build script — and budget an hour-class compile per OS per
  OCCT bump (conda-forge's own CI times for this recipe), cached
  forever after; (c) patch 0004 is proven to set the flags, not proven
  to ship a faster kernel — WP-A verifies that if the source path is
  ever taken. The probe's cap was called here: 2 h 10 min of agent
  time in this session (plus the first session's download/extract).

### 03:10–04:00 (08-20) — review revision (what was measured, in order)

1. `dumpbin /dependents` on `TKDESTEP TKXCAF TKCAF TKService TKV3d
   TKVCAF` — the review's chain confirmed, the memo's refuted. Wrote
   `dll_closure.py`; its closure of the probe exe reproduces the
   earlier `dumpbin` numbers (15 / 30,010,368 B) and the review's
   (`TKDESTEP` alone 23 / 36,697,088 B; modeling + `TKDESTEP` 26 /
   44,790,784 B; the 24-lib list 29 / 49,017,344 B; all three
   `MISSING: FreeImage.dll freetype.dll`).
2. Fetched `freetype` + `freeimage` (`fetch_conda_pkg.py`, sha256
   checked against the listing) → closure still `MISSING` 11 codec
   DLLs → fetched the nine codec packages → `MISSING` 5 → fetched five
   more → `MISSING: none`. 17 packages, 21 non-OCCT DLLs, 11,101,184 B.
3. Added `src/bin/step.rs` (`occt-probe-step`: `write_step` +
   `read_step`); rebuilt (20.4 s; `occt-probe.exe` 332,800 B,
   `occt-probe-step.exe` 243,200 B). Clean-PATH launches via
   `Process.Start`, cwd `C:\Windows\Temp`: modeling exe without
   `Library\bin` → `0xC0000135`; STEP exe with `Library\bin` only →
   `0xC0000135`; + freetype/freeimage dirs only → `0xC0000135`; + all 16
   → exit 0 (`box.step` 15,416 B, round trip 6 faces); bundle of exe +
   47 DLLs (56,135,168 B) with `PATH=System32;Windows` → exit 0.
4. `dump` header asserts added; `dump` re-run twice in two processes and
   cwds → `diff` empty, and identical line for line to the original
   `det\run1.txt` (all 20 hash lines).
5. `throw` added: `Standard_DomainError` from a 0×0×0 box → `fatal
   runtime error: Rust cannot catch foreign exceptions, aborting`, exit
   `0xC0000409`. conda `bld.bat`: `BUILD_RELEASE_DISABLE_EXCEPTIONS=OFF`;
   the binding has exactly two `Result<…>` bridge declarations
   (`poly.rs`, `top_tools.rs`).
6. `occt-sys` configure flags read from the registry crate:
   `USE_FREETYPE=FALSE USE_FREEIMAGE=FALSE` — the own-built artifact has
   no codec closure; conda's `bld.bat` sets `USE_FREEIMAGE:BOOL=ON` and
   leaves `USE_FREETYPE` at its CMake default ON.
7. `build.rs` version gate read: `OCCT_VERSION = (7, 8)`, `minor < 8`
   rejected only — §Q1 and §4c corrected.
8. `cargo search opencascade|occt` + crates.io dependency lists for
   `cadrum` and `occt-wasm` — §Recommendation's survey paragraph.

## Recommendation

**For development and CI now: prebuilt conda-forge shared OCCT 7.8.1
behind a `cicada-geom` `occt` feature, the binding in an own fork that
carries exactly the reviewed patches in
`tools/probes/occt-2026-08/patches/`** (0001 MSVC handle aliases, 0002
BinTools symbol collision, 0003 Windows static system libs; 0004 is for
the source path only) plus the WP-B additions (explicit-flag BinTools
writer, `kicad-parser` dropped, `LGPL-2.1-only` in the manifests).
**For the shipped binary: an own CI-built OCCT artifact (shared or
static) configured with `USE_FREETYPE`/`USE_FREEIMAGE` OFF, produced
once per OCCT bump by a dedicated workflow and fetched by the same
`tools/fetch_occt.py` into the same `DEP_OCCT_ROOT` prefix layout** —
a fetch-table change, not a redesign, scheduled before the first
external release. (Revised 2026-08-20: the first version of this
section recommended conda's libs for shipping too; the measured STEP
closure — 47 DLLs, 56 MB, 16 licences, FreeImage's whole codec stack
loaded to write a STEP file — is what changed it.) Evidence, in the
order it was gathered:

1. **It works on MSVC today** with 25 mechanical source files of rename
   (Q1) — the only real upstream blocker — and nothing else: no build
   script edits, no link-lib additions, no cmake flags for the shared
   path. The detection (`DEP_OCCT_ROOT` → `find_package`) worked first
   try against the conda layout.
2. **It is cheap to build**: 28.5 s cold, 0.7 s relink, a 326 KB
   executable; the CI job is +3 min cold / +30 s warm per OS, with the
   kernel compile nobody has to wait for ever again.
3. **It is shippable without STEP, and expensive with it**: 15 DLLs /
   30.0 MB beside the exe for the modeling closure, proven to launch
   from a clean process. With STEP (Item 3 WP-C's `import_step` /
   `export_step`), conda's build needs 47 DLLs / 56.1 MB — 26 OCCT + 21
   font/codec DLLs from 16 packages — also proven to launch, and proven
   to fail at load (`0xC0000135`) with anything less. No link-list trim
   in the fork changes that: it is `TKDESTEP → TKXCAF → TKV3d →
   TKService → freetype/FreeImage`, compiled into conda's `TKService`.
   An `occt-step` sub-feature keeps the 15-DLL build available for
   STEP-less binaries; the STEP-capable shipping artifact is the
   own-built OCCT with the two `USE_*` switches off (26 OCCT DLLs /
   44.8 MB shared, or one ~31 MB static exe).
4. **Determinism is not a reason to prefer any path**: byte identity
   across processes held for all four shapes in both formats and both
   tessellation buffers, AND across two different OCCT builds by two
   different MSVC versions (conda's `/O2` shared vs the unoptimized
   static source build). The remaining unknown — cross-OS libm /
   FMA — is identical for every option and is answered by the CI job.
5. **The in-cargo source build (`builtin`) is the worse deal on every
   axis measured**: unoptimized it is 4 min here and ships a 2–2.8×
   slower kernel (cmake-rs drops `/O2` on MSVC until patch 0004 forces
   it); optimized it is hour-class per OS (the `/O2` build hung through
   cmake-rs/MSBuild and was killed at 41 min); it does not link on
   Windows without PR #216's libs; static linking brings LGPL §6(a) at
   go-public. Nobody should pay that inside `cargo build`.
6. **An own CI-built prebuilt artifact is NOT needed for v0.1
   development but IS the shipping artifact** (revised 2026-08-20 — the
   first version said it "buys nothing now"). For dev and CI,
   conda-forge provides all four platforms at one build number from one
   feedstock commit, and the own artifact would need the same fetch
   script, feature gate and CI job plus a second pipeline. What it buys
   is exactly what conda's build cannot give: STEP without the 21-DLL
   codec closure and its 16 notices (`USE_FREETYPE`/`USE_FREEIMAGE`
   OFF, as `occt-sys` already configures), the `/O2` flags under our
   control, and independence from conda-forge keeping the 7.8.1 files
   (they have not removed old builds historically; the URLs are
   version-addressed). Build it once per OCCT bump with plain cmake +
   Ninja in a dedicated workflow (hour-class per OS), upload it as a
   release asset, and let `tools/fetch_occt.py` take either source —
   both are `DEP_OCCT_ROOT` prefixes. Static vs shared for that
   artifact is the go-public licence question of risk 1, decided then.

**What else exists (surveyed 2026-08-20 so the fork-vs-seam call is
made against the field; none evaluated beyond metadata and dependency
lists).** `cargo search opencascade|occt`: **`cadrum` 0.8.16** (MIT,
one author, first published 2026-03-29, 2.5k downloads; "statically
linked, headless OpenCASCADE", native + wasm) — its build script depends
on `minreq` + `tar` + `libflate`, i.e. it **downloads at build time**,
which our hermetic CI/deny posture and "fail loudly" rule exclude; its
OCCT version and MSVC story are unrecorded. **`occt-wasm` 3.3.0**
(MIT OR Apache-2.0, one author, first published 2026-05-16; OCCT
compiled to WebAssembly and run in-process under `wasmtime 46`) — no
C++ toolchain and, by WebAssembly's deterministic float semantics,
cross-OS byte identity by construction, but an unmeasured performance
tax on the 3 ms boolean, no threads, and two months of history; worth a
one-day probe only if cross-OS hashes diverge in WP-A. **`brepkit-*`
3.3.7** (same author; a pure-Rust B-rep kernel, `AGPL-3.0-only` —
incompatible with a proprietary binary) and **`truck`** (Apache-2.0,
pure Rust, booleans historically experimental) are not OCCT and not the
row-16 decision. **`chijin`** is deprecated in favour of `cadrum`.
`opencascade` 0.2.0 / `occt-sys` 0.6.0 on crates.io are the stale
releases of the binding we used. Conclusion unchanged: `opencascade-rs`
at `d114250` in an own fork is the only binding with a working,
reviewed MSVC path today; its thinness (risk 2) is why the seam should
become Cicada-owned code rather than a growing fork.

The `Solid` kind hashes its **canonical BinTools V4 bytes written with
`theWithTriangles = false, theWithNormals = false` and a pinned format
version**, after normalizing single-solid compounds — Q2 says that is
well defined and stable; the geometric-summary alternative is not
needed.

### The three biggest risks

1. **Licensing of the glue, not of OCCT.** The binding crates are
   LGPL-2.1 and Rust links them statically; if Cicada is ever
   distributed as a proprietary binary, §6(a) bites on the glue even
   though OCCT itself rides §6(b) as DLLs. Mitigation that costs least
   if decided early: treat the fork as a temporary scaffold and make
   the ~25-function seam Cicada-owned code (the OCCT exception covers
   our object code). This is a Ben decision, not a WP decision; it
   does not block v0.1 because nothing is conveyed while private.
2. **The upstream binding is thin where we need it thick.** One
   evening found two silent defects on the exact path `Solid` depends
   on (the BinTools writer that was secretly the text writer; the
   with-triangles default), an untested MSVC build (PR #230 closed
   unmerged, author's fork deleted), a `Compound`-wrapping `Cut`, an
   edge iterator that double-counts, and `kicad-parser` in the
   dependency tree. Expect every WP-C node to touch the glue and the
   fork to diverge quickly; budget review time for C++ changes
   (fail-loud rule: no `unique_ptr(nullptr)`-style silent failures in
   our additions — the existing `read_brep_bin` returns a null
   `UniquePtr` instead of an error type). **And OCCT exceptions
   terminate the process** (verified 2026-08-20, `occt-probe throw`):
   conda builds with `BUILD_RELEASE_DISABLE_EXCEPTIONS=OFF`, so
   `Standard_Failure` really throws in Release; the binding declares
   only two bridge functions with a cxx `Result`, so for everything
   else cxx generates no try/catch and the C++ exception unwinds into
   Rust — `fatal runtime error: Rust cannot catch foreign exceptions,
   aborting`, exit **`0xC0000409`**. A degenerate `box_from_corners(p,
   p)` is enough (`BRepPrim_GWedge::Shell` throws
   `Standard_DomainError`). In cicada-server that is the whole engine
   gone on one bad boolean input. The seam must declare every throwing
   OCCT call as a cxx `Result` (or catch `Standard_Failure` in the
   glue) so kernel failures arrive as typed errors and Esc/cancel can
   unwind instead of aborting — a WP-B acceptance test, not a note.
3. **Platform and runtime facts still unmeasured.** Linux/macOS
   build + load paths (conda's RPATH/install-name conventions for
   `libTK*.so`/`.dylib`, whether `DYLD_LIBRARY_PATH` survives SIP in
   CI, whether the Unix packages' modeling closure also avoids
   freetype, and what their STEP closure costs — the win-64 answer is
   47 DLLs, so expect the same shape) and cross-OS hash identity are
   all "expected fine, verified nowhere". Plus two smaller ones: conda-forge's OCCT is
   **not pristine** — 16 FreeCAD-ecosystem patches touch `BRepFill`
   (loft/pipe/sweep), `BRepOffset`, `ChFi3d` — so golden hashes pin
   that patched behaviour and a future conda rebuild can move them
   (mitigated by pinning the exact `_103` files); and `MSVCP140.dll`
   is a new runtime requirement on Windows machines without the VC++
   redistributable (mitigated by bundling it — it is redistributable).

## Reproduce

```powershell
# one-time: fetch + extract (sha256 above); pip install zstandard
python %LOCALAPPDATA%\cicada-occt\extract_conda.py occt-7.8.1-novtk_hdfbec02_103.conda occt-7.8.1-win64
# scratch clone of the binding at the pinned rev, with the probe's patches
git clone https://github.com/bschwind/opencascade-rs %LOCALAPPDATA%\cicada-occt\opencascade-rs-patched
git -C %LOCALAPPDATA%\cicada-occt\opencascade-rs-patched checkout d114250a464d50cde1901743d3bf523a099d6887
git -C %LOCALAPPDATA%\cicada-occt\opencascade-rs-patched apply <repo>\tools\probes\occt-2026-08\patches\0001-msvc-handle-aliases.diff
git -C %LOCALAPPDATA%\cicada-occt\opencascade-rs-patched apply <repo>\tools\probes\occt-2026-08\patches\0002-bin-tools-symbol-collision.diff
git -C %LOCALAPPDATA%\cicada-occt\opencascade-rs-patched apply <repo>\tools\probes\occt-2026-08\patches\0003-windows-static-system-libs.diff
# build + run (shared, prebuilt)
$env:CARGO_TARGET_DIR = "%LOCALAPPDATA%\cargo-target-wt\occt-probe"
$env:Path = "C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin;$env:Path"
$env:DEP_OCCT_ROOT = "%LOCALAPPDATA%\cicada-occt\occt-7.8.1-win64\Library"
cargo build --release --manifest-path <repo>\tools\probes\occt-2026-08\Cargo.toml
$env:Path = "%LOCALAPPDATA%\cicada-occt\occt-7.8.1-win64\Library\bin;$env:Path"
& "$env:CARGO_TARGET_DIR\release\occt-probe.exe" smoke
& "$env:CARGO_TARGET_DIR\release\occt-probe.exe" dump <dir>     # twice, diff the output
& "$env:CARGO_TARGET_DIR\release\occt-probe.exe" bench 1000
& "$env:CARGO_TARGET_DIR\release\occt-probe.exe" throw          # expect abort, exit 0xC0000409
# DLL closure of the two binaries (pure Python, no dumpbin needed)
python <repo>\tools\probes\occt-2026-08\dll_closure.py --dir %LOCALAPPDATA%\cicada-occt\occt-7.8.1-win64\Library\bin "$env:CARGO_TARGET_DIR\release\occt-probe.exe"        # 15 / 30,010,368 B, MISSING: none
python <repo>\tools\probes\occt-2026-08\dll_closure.py --dir %LOCALAPPDATA%\cicada-occt\occt-7.8.1-win64\Library\bin "$env:CARGO_TARGET_DIR\release\occt-probe-step.exe"   # 26 TK + MISSING freetype/FreeImage, exit 1
# the STEP runtime closure: fetch the 16 packages of the §setup table (exact --version/--build there), then
#   dll_closure.py --dir <occt>\Library\bin --dir <deps>\<pkg>\Library\bin ... occt-probe-step.exe   -> 26 + 21, MISSING: none
python <repo>\tools\probes\occt-2026-08\fetch_conda_pkg.py --version 2.13.3 --build h0b5ce68_0 freetype
python <repo>\tools\probes\occt-2026-08\fetch_conda_pkg.py --version 3.18.0 --build h8310ca0_22 freeimage
# ... (imath libjpeg-turbo libpng libraw libtiff libwebp-base libzlib openexr openjpeg libdeflate lcms2 lerc liblzma zstd)
# launch with an explicit PATH (never from inside a DLL dir): run_clean.ps1 in the cache dir
```

Artifacts left on this machine (not committed): the package, the
extracted prefix, the patched clone (+ submodule), `det\run{1,2}.txt`,
`det\static*.txt`, `bundle-test\`, the three static target dirs, and
the build logs — all under `%LOCALAPPDATA%\cicada-occt\`
and `%LOCALAPPDATA%\cargo-target-wt\occt-probe*`. From
the review revision: `deps\` (the 17 packages, their listings and
`manifest.tsv`), `det2\` (re-run dumps `runA/runB`, `step{1,2,3}`,
`step_closure.json`), `bundle-test-step\` (exe + 47 DLLs),
`run_clean.ps1` (explicit-PATH launcher).
