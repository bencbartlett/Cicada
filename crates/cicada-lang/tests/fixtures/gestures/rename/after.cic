# cicada 1
sites = scatter(count=100, seed=7)
regions = voronoi(seeds=sites, boundary=board)
grown = grow(cells=each(regions), rate=seeds_rate)
pair = join(a=regions, b=regions)
total = seeds_rate + 1
