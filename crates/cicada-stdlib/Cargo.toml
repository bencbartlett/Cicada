[package]
name = "cicada-stdlib"
description = "The Cicada node catalog: ~130 typed, pure, deterministic node functions"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
cicada-core.workspace = true
cicada-macros.workspace = true

[dev-dependencies]
proptest.workspace = true
# The macros crate keeps zero workspace deps (DAG law), so its compile-fail
# UX is tested from here, where cicada-core is a real dependency.
trybuild.workspace = true

[lints]
workspace = true
