[package]
name = "cicada-core"
description = "Cicada value model: hashing, interning, axes, Optional slots, ProjectConfig"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
blake3.workspace = true
glam.workspace = true
inventory.workspace = true
thiserror.workspace = true

[dev-dependencies]
# Dev-only: the macros' compile-fail UX lives here (tests/macro_ui.rs) —
# core + macros is the SMALLEST closure that exercises them; hosting it in
# cicada-stdlib made every trybuild scratch build compile the geometry
# kernels. No cycle: cicada-macros has zero workspace deps by law.
cicada-macros.workspace = true
trybuild.workspace = true

[lints]
workspace = true
